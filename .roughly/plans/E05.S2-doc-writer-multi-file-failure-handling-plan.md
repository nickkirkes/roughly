> **Status:** Historical — implemented and merged in commit 9fbaf176b041b79e3bc21518ec63f78d1b0b63fd on 2026-05-29. This plan was an active build/fix artifact; treat as historical reference only.

# Implementation Plan: E05.S2 doc-writer multi-file failure-handling — AC amendment + completion

Plan-format-version: 1

## File Table

| File | Action | Task(s) |
|------|--------|---------|
| `agents/doc-writer.md` | Modify (typo fix + clause relocation + section insertion) | T1, T2 |
| `docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md` | Modify (3 back-pointer note inserts) | T3 |
| `CHANGELOG.md` | Modify (1 `### Changed` entry insert) | T4 |

## Tasks

### T1: Fix `docs/adr/` typo at L23 (~1 min)

**Files:** `agents/doc-writer.md`
**Action:** Single Edit changing the singular `docs/adr/` path reference to plural `docs/adrs/`.
**Details:**
- The current L24 reads: `` - `docs/adr/` — Create ADRs for significant architectural decisions (if directory exists)``
- The `docs/adr/` (singular) string occurs nowhere else in the file (discovery confirmed).
- Use Edit with `old_string: "docs/adr/"` and `new_string: "docs/adrs/"`.
- Rationale: the only ADR directory on disk is `docs/adrs/` (plural). E05.S2 spec rolls in this typo fix per OQ10.
**Verify:**
- `grep -Fn "docs/adr/" agents/doc-writer.md` returns no match.
- `grep -Fn "docs/adrs/" agents/doc-writer.md` returns at least 1 match.
**UI:** no

---

### T2: Relocate failure-handling clause into new `## Failure handling` section, with AC2/AC3/AC4 strengthening (~6 min)

**Files:** `agents/doc-writer.md`
**Depends on:** T1
**Action:** Two coordinated Edit calls. (a) Delete the multi-file failure-handling sub-bullet currently inside Process step 5. (b) Insert a new `## Failure handling` top-level section before `## Writing Guidelines` containing the relocated prose, strengthened per AC2/AC3/AC4. Together these implement AC1 (relocation + gate-override prefix removal), AC2 (anchoring strength: MUST + code-fenced template + post-emit self-check), AC3 (empty-error fallback), AC4 (all-fail branch + branch-selection rule), and preserve AC7 (Process steps 1–6 byte-identical except for the deleted sub-bullet).

**Details:**

**Edit (a) — Delete the multi-file failure-handling sub-bullet from Process step 5.**

Use Edit with `old_string` matching the full sub-bullet line plus its trailing newline. The verbatim text of the sub-bullet (discovery confirmed at L35 of `agents/doc-writer.md`):

````
   - **Multi-file failure handling (always — overrides step 5's outer gate):** When writing multiple files in one dispatch, invoke `Edit` per file and capture each outcome. On any failure, do NOT roll back successful writes — never claim full success. Emit this exact summary: `"doc-writer: partial success — wrote to: <comma-separated list of successful paths>; failed to write: <comma-separated list of failed paths with one-line failure reason each, format '<path>: <reason from Edit error output>'>."`
````

Set `new_string` to an empty string (or, if the trailing newline cannot be matched, an empty single-line replacement followed by a follow-up Edit removing the now-blank line — the goal is no orphan blank line between the test-integration sub-bullet at L33 and `6. **Deduplicate**` at L35).

After Edit (a), `grep -Fn "always — overrides step 5's outer gate" agents/doc-writer.md` must return no match (gate-override prefix removed per AC1). The Process step 5 sub-bullet list contracts from three sub-bullets to two (Organize suggestion + Test-integration suggestion).

**Edit (b) — Insert new `## Failure handling` section before `## Writing Guidelines`.**

Use Edit anchored on the unique line `## Writing Guidelines` (discovery confirmed unique at L38).

`old_string`:
````
## Writing Guidelines
````

`new_string` (literal content to insert — note the inner triple-backtick fences are part of the file content):

````
## Failure handling

When writing multiple files in one dispatch, invoke `Edit` per file and capture each outcome. On any failure, do NOT roll back successful writes — never claim full success.

Your return summary MUST literally begin with one of the two templates below. Format your return summary EXACTLY as this string, substituting only the placeholders. Use the partial-success template when ≥1 write succeeded; use the all-fail template when 0 writes succeeded.

```
doc-writer: partial success — wrote to: <comma-separated list of successful paths>; failed to write: <comma-separated list of failed paths with one-line failure reason each, format '<path>: <reason from Edit error output>'>.
```

```
doc-writer: all writes failed — <comma-separated list of failed paths with one-line failure reason each, format '<path>: <reason from Edit error output>'>.
```

If Edit's error output is empty for a failed path, write '(no error output)' as the reason.

Before returning, confirm your summary's first line is `doc-writer: partial success — …` or `doc-writer: all writes failed — …`.

## Writing Guidelines
````

**Word-cap measurement and trim escape hatch:**

After both edits, run `wc -w agents/doc-writer.md`. Projected outcome: **649 words** (1 word of headroom under the cap; net delta +92 words: 557 baseline + 167 new section − 75 deleted L35 sub-bullet = 649). Required: **≤ 650 words** (AC5). Apply the first-tier trim if the actual count is 650–654.

If the count exceeds 650:
1. First-tier trim: in the new section's para 1, remove ` — never claim full success` (5 words). The MUST-language sentence + code-fenced template substitution enforces this implicitly; the hedge is belt-and-suspenders.
2. Second-tier trim: in the new section's para 2, drop the leading "Your return summary " from "Your return summary MUST literally begin with one of the two templates below." (3 words); resulting sentence reads "MUST literally begin with one of the two templates below." — still satisfies AC2 (a) verify (`grep -Fn "MUST literally"` returns match) and the imperative force.
3. Do NOT trim the AC2 (b) preamble verbatim phrase ("Format your return summary EXACTLY as this string, substituting only the placeholders:") — it is spec-required.
4. Do NOT trim the AC2 (c) self-check sentence — it is spec-required.
5. Do NOT trim the templates themselves — they are spec-locked.
6. Do NOT trim ` '(no error output)'` from the AC3 fallback — it is the AC3 grep anchor.
7. Do NOT trim "all writes failed" from the AC4 template — it is the AC4 grep anchor.

**Verify:**
- `wc -w agents/doc-writer.md` returns a value ≤ 650 (AC5).
- `grep -Fn "## Failure handling" agents/doc-writer.md` returns 1 match at a top-level (`##`) heading (AC1).
- `grep -Fn "MUST literally" agents/doc-writer.md` returns at least 1 match (AC2).
- `grep -Fn "(no error output)" agents/doc-writer.md` returns at least 1 match (AC3).
- `grep -Fn "all writes failed" agents/doc-writer.md` returns at least 1 match (AC4).
- `grep -Fn "6. **Deduplicate" agents/doc-writer.md` returns exactly 1 match (AC7 — step 6 unchanged in number and label).
- `grep -Fn "always — overrides step 5's outer gate" agents/doc-writer.md` returns no match (gate-override prefix removed per AC1).
- `git diff agents/doc-writer.md` inspection: no content removed or modified outside (i) the L23 typo correction (T1), (ii) the deleted L34 sub-bullet (T2 edit a), and (iii) the inserted `## Failure handling` section (T2 edit b). Process steps 1–4 and step 6 byte-identical pre/post (AC7).

**UI:** no

---

### T3: Add `Amended in E05.S2` back-pointer notes to E04 epic (~3 min)

**Files:** `docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md`
**Action:** Three Edit calls inserting the standardized back-pointer note (per CONTRIBUTING.md L73 format) immediately after the E04.S8 AC2, AC4, and AC5 bullet entries.
**Details:**

The back-pointer text to insert at each of three sites (verbatim, per CONTRIBUTING.md L73 convention):

`**Amended in E05.S2 — see E05.S2 for the corrected contract.**`

Insertion sites (discovery-confirmed line numbers in the E04 epic):

**Site 1 — after AC2 at L440.** Locate the AC2 line beginning with `- **AC2 — Strictly additive.** Existing Process steps 1–4 and 6+ byte-identical pre/post-edit;` and insert the back-pointer note as the next line (with a leading blank line for visual separation).

Use Edit anchored on the unique trailing portion of the AC2 line. Suggested `old_string`:
```
HTML comment markers around the new clause are acceptable and do not trigger a violation.
```
Suggested `new_string`:
```
HTML comment markers around the new clause are acceptable and do not trigger a violation.

  **Amended in E05.S2 — see E05.S2 for the corrected contract.**
```
(Two-space leading indent to align with the AC bullet's continuation indent; matches the visual style of nested back-pointer notes per the CONTRIBUTING.md convention. If the surrounding entries use a different indent, match theirs.)

**Site 2 — after AC4 at L444.** Locate the AC4 line beginning with `- **AC4 — Two-part-gate preserved.**` Insert the back-pointer note as the next line. Anchor on the unique trailing fragment of the AC4 line. Suggested `old_string`:
```
Verification: read Process step 5 pre/post; confirm the two-part-gate conditional is unchanged.
```
Suggested `new_string`:
```
Verification: read Process step 5 pre/post; confirm the two-part-gate conditional is unchanged.

  **Amended in E05.S2 — see E05.S2 for the corrected contract.**
```

**Site 3 — after AC5 at L446.** Locate the AC5 line beginning with `- **AC5 — Partial-success summary format specified, not free-form.**` Insert the back-pointer note as the next line. Anchor on the unique trailing fragment of the AC5 line. Suggested `old_string`:
```
Implementation does not invent alternate formats.
```
Suggested `new_string`:
```
Implementation does not invent alternate formats.

  **Amended in E05.S2 — see E05.S2 for the corrected contract.**
```

**Pre-flight check:** before each Edit, verify that the `old_string` is unique in the file (run `grep -Fc "<old_string>" docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md` — must return exactly 1). If any anchor is non-unique, widen the `old_string` to include preceding context until uniqueness is achieved.

**Indentation note:** the two-space leading indent suggested above is conventional for sub-bullets that visually continue an AC bullet. If the E04 file uses a different convention for similar nested annotations (e.g., 4 spaces, or none), match the file's existing style. Inspect the file's existing AC sub-structure before choosing the indent.

**Verify:**
- `grep -Fc "Amended in E05.S2" docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md` returns exactly 3 (one per insertion site).
- `grep -Fn "Amended in E05.S2" docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md` reports three line numbers each immediately following an AC2/AC4/AC5 entry inside the E04.S8 section (visually contiguous with the amended AC).

**UI:** no

---

### T4: Add `### Changed` CHANGELOG entry (~2 min)

**Files:** `CHANGELOG.md`
**Action:** Single Edit inserting a new bullet under the existing `### Changed` subsection of `## [Unreleased]`. Per CONTRIBUTING.md L74, the entry must cross-reference both the original AC location (E04 epic + E04.S8) and the amending location (this epic + E05.S2).
**Details:**

Discovery confirmed that `### Changed` lives at L13 of `CHANGELOG.md` and currently contains two bullets (L15 E05.S4, L17 E05.S1). The existing `### Changed` block uses **newest-first** ordering — L15 = E05.S4 (PR #52) is newer than L17 = E05.S1 (PR #50). Insert the new E05.S2 bullet at the top of the `### Changed` list, above the existing E05.S4 entry.

Suggested entry (single bullet, multi-clause):

```
- **E05.S2 — doc-writer multi-file failure-handling contract revision.** Relocates the failure-handling clause from `agents/doc-writer.md` Process step 5 into a new `## Failure handling` top-level section. Strengthens the partial-success template anchoring (MUST-language imperative, code-fenced template, post-emit self-check), adds an empty-error fallback, and adds an all-fail branch template with branch-selection rule. Amends E04.S8 AC2/AC4/AC5 (original entries in `docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md` gain back-pointer notes; corrected contract is the E05.S2 entry of `docs/planning/epics/E05-doc-writer-hardening-and-spec-quality-gates.md`). Convention codified in `CONTRIBUTING.md` `## Cross-epic AC amendments`.
```

Use Edit anchored on the `### Changed` heading line (unique in `## [Unreleased]`). `old_string` is the literal text `### Changed`; `new_string` is `### Changed` + a blank line + the new E05.S2 bullet + a blank line (so that the new bullet appears at the top of the list, above the existing E05.S4 entry). Inspect L13–L17 first to confirm the heading line's exact text and adjacent blank-line conventions; adapt the `new_string` whitespace to match.

**Style match:** follow the bold-titled bullet pattern (`- **E0x.Sx — <title>.** <prose>`) used by L15 and L17.

**Verify:**
- `grep -Fc "E05.S2" CHANGELOG.md` returns at least 1 (the new entry).
- `grep -Fn "E04.S8" CHANGELOG.md` includes a line near the new E05.S2 entry (confirms cross-reference to original AC location per CONTRIBUTING.md L74).
- `grep -Fn "Cross-epic AC amendments" CHANGELOG.md` returns at least 1 match (confirms reference to the codified convention).
- Visual inspection: new bullet renders as a peer of the existing E05.S1 and E05.S4 bullets under `### Changed` of `## [Unreleased]`.

**UI:** no

---

## Blast Radius

- **Do NOT modify** any other agent file (`agents/code-reviewer.md`, `agents/discovery.md`, `agents/epic-reviewer.md`, `agents/investigator.md`, `agents/silent-failure-hunter.md`, `agents/static-analysis.md`, `agents/agent-preamble.md`).
- **Do NOT modify** `CONTRIBUTING.md` — the `## Cross-epic AC amendments` subsection is already codified by E05.S3; E05.S2 inherits and applies it but does not edit it (per spec L104).
- **Do NOT modify** any skill file under `skills/`.
- **Do NOT modify** any ADR under `docs/adrs/`, any setup template, any verify-all.sh hook, or any test fixture.
- **Do NOT renumber Process steps** in `agents/doc-writer.md` — step 6 `**Deduplicate**` MUST retain its `6.` prefix; AC7 verify (`grep -Fn "6. **Deduplicate"`) depends on it.
- **Do NOT touch `.roughly/known-pitfalls.md`** — the L78 entry referencing the original T2 failure is historical record per the back-pointer convention (CONTRIBUTING.md L72).
- **Watch for word-cap budget:** measure `wc -w agents/doc-writer.md` after T2; apply trim escape hatch if > 650. The realistic projection is **649 words** (557 baseline + 167 inserted − 75 deleted) — exactly 1 word of headroom under the 650 cap. The trim escape hatch is likely to fire if the implementer's verbatim transcription introduces even 2 extra words via paragraph-fill differences or smart-quote substitution. Apply first-tier trim (remove ` — never claim full success`) at counts 650–654; second-tier trim (remove leading `Your return summary `) at counts 655+.
- **Watch for fence-nesting in markdown:** the inserted `## Failure handling` section contains triple-backtick code fences for the templates. These nest fine in the source markdown and render correctly — the Edit tool inserts literal text.

## Conventions

- **AC6 back-pointer format** per `CONTRIBUTING.md` L73 — verbatim: `**Amended in <new-story-id> — see <new-story-id> for the corrected contract.**` (substitute `E05.S2` for `<new-story-id>` in both positions).
- **AC2 anchoring** combines three concurrent reinforcements per `.roughly/known-pitfalls.md` L72: MUST-language imperative + code-fenced template + post-emit self-check. All three required (single-mechanism anchoring proved insufficient in E04.S8 T2 — see known-pitfalls.md L78).
- **Section heading style:** new top-level `##` heading matches existing file style (no decorative characters; sentence-case head).
- **Agent dispatch model:** Sonnet (per ADR-008).
- **Plan-format-version: 1** — task structure follows the per-task File/Action/Details/Verify/UI shape per `skills/build/SKILL.md` Stage 3 schema.
- **Trim escape hatch (per AC5 spec language):** retained for the unlikely case of a larger-than-projected delta; first/second-tier trims defined explicitly in T2 to avoid implementer guesswork.

## Task ordering rationale

- T1 first: smallest, lowest-risk change. Reduces the diff footprint observed by subsequent tasks' contextual reads.
- T2 second: largest single change but localized to one file. Must follow T1 because both touch `agents/doc-writer.md` (sequential per-file edits avoid Edit anchor collisions).
- T3 third: independent file (`E04` epic); could theoretically run before T2, but ordering after T2 simplifies the "all doc-writer.md changes are done" mental model.
- T4 last: CHANGELOG is the canonical "summary of all preceding changes" — naturally lands after the substantive edits are in place so the bullet's prose accurately reflects the final state.

No task has a hard dependency on any task beyond its declared `Depends on:` field. T3 and T4 could run in parallel after T2 completes; the build pipeline runs them sequentially per the per-task-subagent model.

## Notes for synthetic test (Stage 7 — NOT executed in Stage 5)

The T2 synthetic test from E04.S8 is prose-only; no harness on disk. Stage 7 must construct the test by hand:

1. **Partial-success scenario:** `chmod 000 .roughly/known-pitfalls.md` (or another writeable target in `Files You May Update`), dispatch doc-writer with both `.roughly/known-pitfalls.md` and `CLAUDE.md` as write targets, observe whether the return summary's first line matches the verbatim partial-success template. Revert chmod after.
2. **All-fail scenario:** `chmod 000` on both targets, dispatch, observe whether the return summary's first line matches the verbatim all-fail template. Revert chmod after.

Outcome classification per AC2:
- FULL PASS (both scenarios verbatim) → Risk 1 closes.
- PARTIAL PASS (one of two) → ship S2 with documented partial result; failing scenario becomes v0.1.8 candidate.
- FULL FAIL → ship S2 with documented failure; Risk 1 v0.1.8 fallback (programmatic mechanism) promoted to must-do.

Per spec: do not block release on Risk 1 close.
