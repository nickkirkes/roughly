> **Status:** Historical — implemented and merged in commit a5d07ad8d385181fe399f15ecb11a93d20c74996 on 2026-06-03. This plan was an active build/fix artifact; treat as historical reference only.

# Implementation Plan: E06.S1 — doc-writer AC4 all-fail-branch anchoring tightening + cap-relief trim + cross-epic re-amendment convention extension

Plan-format-version: 1

**Epic:** `docs/planning/epics/E06-anchoring-closure-and-ci-coverage.md` lines 63–157
**Branch:** `feat/E06.S1-doc-writer-ac4-all-fail-branch-anchoring`
**Pre-edit state:** `agents/doc-writer.md` 649/650 words, 80 lines (verified against HEAD 2026-06-03)

## File Table

| File | Action | Task(s) |
|------|--------|---------|
| `agents/doc-writer.md` | Multi-edit (trim + AC1 additions) | T1 |
| `docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md` | Additive: 1 line below L452 | T2 |
| `docs/planning/epics/complete/E05-doc-writer-hardening-and-spec-quality-gates.md` | Additive: 1 line below L117 | T3 |
| `CONTRIBUTING.md` | Additive: extend section after L76 | T4 |
| `CHANGELOG.md` | Additive: new `## [0.1.8]` section above L3 | T6 |
| `/tmp/doc-writer-t2-sandbox/` | T2 synthetic test (ephemeral, not in repo) | T5 |

## Trim Table (AC2 mandatory artifact)

Per AC2.iii: 4-column trim-table with line range, word count, semantic role, preserved-elsewhere rationale.

| Line range (pre-edit) | Words removed | Semantic role | Preserved-elsewhere rationale |
|-----------------------|---------------|---------------|-------------------------------|
| L59–72 (entire `## Writing Guidelines` section: heading + Pitfalls format block + Conventions format block) | 35 | Illustrative authoring format templates for pitfall/convention entry structure (`### [Short title]` / `**Symptom:** / **Cause:** / **Fix:**` and `- [convention statement — imperative, specific, actionable]`) | Not load-bearing for any of the 4 coverage groups (E04.S8.AC1 failure-mode enumeration, E05.S2.AC2 three-form anchoring at L41/L48/L57, E05.S2.AC3 `(no error output)` fallback at L55, E05.S2.AC4 all-fail template at L52 + branch-selection rule). Format is implicitly conveyed by the existing entries the runtime LLM encounters when reading `.roughly/known-pitfalls.md` at Step 1 of Process. |
| L74 (`## Rules` heading) | 2 | Section heading for Rules block | Section removed entirely; heading has no independent semantic load. |
| L76 (`- Keep entries concise — 2-4 lines per pitfall, 1 line per convention.`) | 13 | Entry-length behavioral norm | Not in any coverage group. Concise-entry behavior is structurally encouraged by the failure-handling section's MUST-templated short summaries and reinforced by existing short entries in `.roughly/known-pitfalls.md` (Step 1 read). |
| L77 (`- Place entries in the correct section of known-pitfalls.md (Domain-Specific, Data & State, Integration, Build & Deploy, Testing).`) | 18 | Specific section-name list for known-pitfalls.md placement | Not in any coverage group. Step 3 "Categorize" at L30 covers the general categorize-by-type instruction. Specific section names are visible to the runtime LLM via Step 1's Read of `.roughly/known-pitfalls.md` (the section headers ARE the categorization scheme). |
| L78 (`- Don't duplicate existing entries — check first.`) | 8 | Deduplication instruction | Covered by Step 6 `**Deduplicate** — Don't add if something equivalent already exists` at L35 (preserved byte-identical per AC7's `grep -Fn "6. **Deduplicate"` verify). |
| L79 (`- Don't remove existing content — only add or refine.`) | 10 | No-removal behavioral norm | Not in any coverage group. The doc-writer is dispatched to ADD documentation; removal/refine isn't part of any AC contract. |
| L80 (`- If unsure whether something warrants documentation, err toward documenting it.`) | 11 | Authoring heuristic (err-toward-documenting norm) | Not in any coverage group. The doc-writer's scope is defined by dispatch input and the four coverage groups (E04.S8.AC1 (a)–(f); E05.S2.AC2/AC3/AC4); the err-toward-documenting heuristic is not load-bearing for any of these. |

**Total trim:** 97 words (Writing Guidelines 35 + Rules 62).
**AC1 additions (epic-verbatim phrasing — see T1 details):** ~95 words.
**Net delta:** 649 − 97 + 95 = **647 words**. Below 650 cap with 3-word slack.

**Coverage-group invariant preservation (AC2 verify):** post-edit `wc -w agents/doc-writer.md` ≤650 AND
- (1) E04.S8.AC1 (a)–(f) failure-mode coverage: read-fail (L20–22 Step 1), CLAUDE.md absent (Step 1 fallback behavior), missing parent directory (L22 Step 2 `mkdir -p`), near-cap target file (L23 Step 2 word-cap warning), Edit error empty/missing (L55 `(no error output)` fallback), all writes failed (L51–53 all-fail template + L41 branch-selection rule). All retained.
- (2) E05.S2.AC2 three-form anchoring: L41 universal MUST (byte-identical), L48 partial-success fence content (byte-identical), L57 universal first-line self-check (byte-identical). All retained.
- (3) E05.S2.AC3 `(no error output)` fallback at L55. Retained byte-identical.
- (4) E05.S2.AC4 all-fail template at L52 (byte-identical) + branch-selection rule at L41 (byte-identical; AC1.a adds an additional standalone restatement; original placement preserved).

## Tasks

### T1: Trim agents/doc-writer.md + apply AC1 three-form layered anchoring (~10 min)

**Files:** `agents/doc-writer.md`
**Action:** Single-file multi-edit: (1) trim `## Writing Guidelines` + `## Rules` sections per the trim table above; (2) add AC1(a) standalone branch-selection MUST below L41 paragraph; (3) add AC1(b) own all-fail-specific MUST line above the L51 all-fail code fence open; (4) add AC1(a) paired self-check below L57; (5) add AC1(c) all-fail-specific self-check below the AC1(a) self-check.

**Details:**

**Phase 1 — Trim first (atomic).** Remove L59 through L80 inclusive (entire `## Writing Guidelines` section L59–72 + entire `## Rules` section L74–80 including all 5 bullets). The file has 80 lines pre-edit (terminated by trailing newline). Post-trim the file ends at L58 (the universal post-emit self-check line is L57; trailing newline makes the file 58 lines).

**Phase 2 — AC1(a) MUST insertion (epic-verbatim per L84).** Below the L41 paragraph (after the existing blank line L42, before the all-success fence open at L43), insert a new paragraph block. Text verbatim from epic L84:

```
Your branch selection MUST pick the all-fail template when 0 writes succeeded; the partial-success template when ≥1 succeeded and ≥1 failed; no template when 0 failed (free-form summary allowed).

```

(Note: the new paragraph is followed by a blank line before the all-success fence open at original-L43 — preserves existing inter-block spacing. The "no template when 0 failed (free-form summary allowed)" clause preserves the all-success case's free-form intent per the epic — this is mandatory for the all-fail and partial-success cases only, free-form for the all-success case.)

**Phase 3 — AC1(b) MUST insertion (epic-verbatim per L86).** Above the L51 all-fail code fence open (after the existing blank line L50, before the fence open), insert a new paragraph block. Text verbatim from epic L86:

```
Your return summary MUST literally begin with `doc-writer: all writes failed —` when 0 writes succeeded.

```

(The MUST line is followed by a blank line before the all-fail fence open — preserves spacing.)

**Phase 4 — AC1(a) self-check insertion (epic-verbatim per L84).** Immediately after L57 (the existing universal self-check line), append a blank line and a new self-check line. Text verbatim from epic L84:

```

Before returning, confirm you selected the template matching your observed outcome (0 succeeded → all-fail; ≥1 succeeded and ≥1 failed → partial-success).
```

**Phase 5 — AC1(c) self-check insertion (epic-verbatim per L88).** Immediately after the AC1(a) self-check line, append a blank line and a new self-check line. Text verbatim from epic L88:

```

If you observed 0 successful writes, confirm your first line begins with `doc-writer: all writes failed —`, NOT `doc-writer: partial success — wrote to: (none); …` (the v0.1.7 misfire pattern).
```

**Edit ordering is mandatory:** Phase 1 (trim) must precede Phases 2–5 (additions), because mid-edit state with additions before trim would exceed the 650 word cap and trigger verify-all.sh failure during any intermediate state check. Use the Edit tool's replace operations sequentially; do NOT interleave.

**Byte-identical preservation contract (AC7):**
- L41 (universal MUST sentence): unchanged
- L44 (all-success template inside fence): unchanged
- L48 (partial-success template inside fence): unchanged
- L52 (all-fail template inside fence): unchanged
- L55 (`(no error output)` fallback): unchanged
- L57 (existing universal post-emit self-check): unchanged
- L35 (`6. **Deduplicate** — Don't add if something equivalent already exists`): unchanged
- L37 (`## Failure handling` heading): unchanged
- The bidirectional-sync comment in L33 (`PITFALLS_ORGANIZE_THRESHOLD`): unchanged

**Verify:**
1. `wc -w agents/doc-writer.md` returns a value ≤650 (target ~636).
2. `grep -Fn "MUST pick the all-fail template when 0 writes succeeded" agents/doc-writer.md` returns 1 match positioned below L41 (AC1(a) verify).
3. `grep -Fn "confirm you selected the template matching your observed outcome" agents/doc-writer.md` returns 1 match positioned below the existing L57 self-check (AC1(a) verify).
4. ``grep -Fn 'MUST literally begin with `doc-writer: all writes failed —`' agents/doc-writer.md`` returns 1 match positioned immediately above the all-fail code fence (AC1(b) verify).
5. ``grep -Fn 'NOT `doc-writer: partial success — wrote to: (none)' agents/doc-writer.md`` returns 1 match in the self-check region (AC1(c) verify).
6. `grep -Fn "Pick template by outcome: 0 failed" agents/doc-writer.md` still returns 1 match at L41 (original branch-selection rule preserved in universal MUST context — AC7).
7. `grep -Fn "6. **Deduplicate" agents/doc-writer.md` returns 1 match (AC7 step-6 preservation).
8. `grep -Fn "## Failure handling" agents/doc-writer.md` returns 1 match at top-level heading position (AC7).
9. `grep -Fn "(no error output)" agents/doc-writer.md` returns 1 match (AC7 — empty-error fallback preserved).
10. `grep -c "^## Writing Guidelines" agents/doc-writer.md` returns 0 (trim verify).
11. `grep -c "^## Rules" agents/doc-writer.md` returns 0 (trim verify).

**UI:** no

---

### T2: Add forward-pointer line to E04.S8.AC5 in E04 epic (~2 min)

**Files:** `docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md`
**Depends on:** None
**Action:** Insert one new forward-pointer line immediately below the existing back-pointer at L452, preserving the L452 back-pointer (and the unrelated L442, L448 back-pointers) byte-identical.

**Details:** The existing back-pointer at L452 is (with leading 2-space indent):
```
  **Amended in E05.S2 — see E05.S2 for the corrected contract.**
```

Use Edit with `old_string` containing the L452 back-pointer plus enough surrounding context to uniquely identify the AC5 location specifically (NOT the L442 or L448 back-pointers). One unique anchor: the line at L451 (AC5's closing prose tail) followed by L452 (back-pointer). Read L450–454 from the file to get exact `old_string` context.

`new_string` = same context with the addition of one new line immediately below the back-pointer:
```
  **Amended in E05.S2 — see E05.S2 for the corrected contract.**
  **Re-amended in E06.S1 — see E06.S1 for the latest contract.**
```

(2-space indent matching existing back-pointer; bold formatting; standalone line; no trailing punctuation beyond the period inside the bold.)

**Verify:**
1. `grep -Fc "Amended in E05.S2" docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md` returns exactly 3 (L442, L448, L452 back-pointers all preserved unchanged).
2. `grep -Fc "Re-amended in E06.S1" docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md` returns exactly 1 (new forward-pointer at AC5 location only — not at AC2 or AC4 per AC5(a) scope-clarification).
3. The new forward-pointer line is immediately below the L452 back-pointer (after the insertion the back-pointer is at its new position; the forward-pointer is at the next line).

**UI:** no

---

### T3: Add forward-pointer line below E05.S2.AC4 prose in E05 epic (~2 min)

**Files:** `docs/planning/epics/complete/E05-doc-writer-hardening-and-spec-quality-gates.md`
**Depends on:** None
**Action:** Insert one new forward-pointer line immediately below L117 (E05.S2.AC4 prose), preserving the L117 AC4 prose byte-identical per AC5(b)'s no-edit-in-place rule.

**Details:** L117 ends with the tail `... branch-selection rule present.` (the AC4 verify command summary). This tail is uniquely identifying in the file.

Use Edit with `old_string` = L117's tail text (e.g., the unique phrase ending L117) and `new_string` = same tail + newline + the forward-pointer line.

Forward-pointer line text (verbatim from epic):
```
  **Re-amended in E06.S1 — see E06.S1 for the latest contract; this entry preserved as historical record.**
```

(2-space indent for bullet continuation; bold; standalone line below the AC4 prose.)

**Verify:**
1. `grep -Fn "Re-amended in E06.S1" docs/planning/epics/complete/E05-doc-writer-hardening-and-spec-quality-gates.md` returns ≥1 match positioned below E05.S2.AC4 prose.
2. The L117 AC4 prose text itself is unchanged byte-for-byte (compare via `git diff` — only insertion of new line below).

**UI:** no

---

### T4: Extend CONTRIBUTING.md `## Cross-epic AC amendments` with multi-hop re-amendment convention (~3 min)

**Files:** `CONTRIBUTING.md`
**Depends on:** None (lands convention before T2/T3 application artifacts cite it — but order does not matter operationally since the convention is also documented in the epic file as the canonical source for this story; CONTRIBUTING.md is the project-level codification)
**Action:** Append the multi-hop re-amendment convention block after L76 (the end of the existing single-hop convention) and before the blank line preceding `## Tooling Pitfalls` at L78.

**Details:** The verbatim convention text from epic L117–119 is tagged `verbatim:` — it must land byte-for-byte modulo whitespace and link formatting (per epic resolves Polish 10). The text to insert (with surrounding blank lines for paragraph separation):

```

When an already-amended AC is itself re-amended from a later epic, four artifacts must land together: (1) the latest amender's epic entry remains the canonical source of the corrected AC text; (2) the original epic entry's existing back-pointer is **preserved unchanged** (preserves the trail's intermediate hop for discovery) AND a **new forward-pointer line** is added **immediately below** the existing back-pointer in the form `**Re-amended in <new-id> — see <new-id> for the latest contract.**`; (3) intermediate amenders also gain a forward-pointer line below their amended-here entry's AC prose in the form `**Re-amended in <new-id> — see <new-id> for the latest contract; this entry preserved as historical record.**` — intermediate-amender entries' AC prose MUST NOT be edited in-place; additive forward-pointer lines placed adjacent to the AC prose are explicitly permitted and required by this rule; (4) the CHANGELOG `### Changed` entry documents the full re-amendment chain (original → intermediate → latest) and references all four trail artifacts by file path and section anchor (original epic's preserved back-pointer + new forward-pointer, intermediate amender's forward-pointer, latest amender's canonical entry). **Recursive application:** for chains of 3+ hops, every prior amender's pointers are preserved; only the new latest-amender forward-pointer is added (the chain remains fully discoverable through preserved hops). First multi-hop instance: E04.S8.AC5 (original) → E05.S2.AC4 (intermediate) → E06.S1 (latest amender).
```

Insert this as a new paragraph (with leading blank line for separation from L76's "Canonical first instance:" line) between L76 and L77 (the blank line before `## Tooling Pitfalls`).

Use Edit with `old_string` = the unique tail of L76 (`Canonical first instance: ...` + newline + the blank line) followed by `## Tooling Pitfalls`, and `new_string` = same context with the multi-hop block inserted before `## Tooling Pitfalls`.

**Verify:**
1. `grep -Fn "Re-amended in" CONTRIBUTING.md` returns ≥1 match in the `## Cross-epic AC amendments` subsection.
2. `grep -Fn "preserved unchanged" CONTRIBUTING.md` returns ≥1 match in the same subsection.
3. The convention text contains the four-artifact requirement, the recursive-application rule, and the named first-instance trail.
4. The existing single-hop convention (L70–76) is byte-identical post-edit (verify via `git diff` — only additions, no edits to existing lines).
5. `## Tooling Pitfalls` heading position is preserved (no edit to L78+).

**UI:** no

---

### T5: T2 synthetic re-run (both scenarios) (~5 min)

**Files:** `/tmp/doc-writer-t2-sandbox/` (ephemeral, not committed)
**Depends on:** T1 (the doc-writer.md edits must land before the test runs against the post-edit anchoring)
**Action:** Execute T2 synthetic test for both scenarios (partial-success, all-fail) using the E05 audit reproduction setup at audit file L221–248. Capture both transcripts. Classify per three-outcome rule (FULL PASS / PARTIAL PASS / FULL FAIL). Cleanup after run.

**Details:**

**Setup (shared by both scenarios):**

```bash
SANDBOX=/tmp/doc-writer-t2-sandbox
mkdir -p "$SANDBOX"
cat > "$SANDBOX/pitfalls-stub.md" << 'EOF'
# T2 Test Pitfalls
## Testing
(none yet)
EOF
cat > "$SANDBOX/claude-stub.md" << 'EOF'
# T2 Test Project
## Conventions
(none)
EOF
```

**Scenario 1 — partial-success (regression check):**
1. `chflags uchg "$SANDBOX/claude-stub.md"` (block writes to one file only).
2. Dispatch the `roughly:doc-writer` subagent (via `Agent` tool with `subagent_type: roughly:doc-writer`) with a task instructing it to write a test pitfall entry to `$SANDBOX/pitfalls-stub.md` (writable, should succeed) AND a test convention entry to `$SANDBOX/claude-stub.md` (locked, should fail with EPERM).
3. Capture the subagent's return summary. Verify the first line literally matches: ``doc-writer: partial success — wrote to: <path>; failed to write: <path>: <reason>.`` (verbatim partial-success template).
4. `chflags nouchg "$SANDBOX/claude-stub.md"` (unlock for cleanup).

**Scenario 2 — all-fail (primary test):**
1. `chflags uchg "$SANDBOX"/*.md` (block writes to both files).
2. Dispatch the `roughly:doc-writer` subagent with a task instructing it to write to both files (both should fail with EPERM).
3. Capture the return summary. Verify the first line literally matches: ``doc-writer: all writes failed — <path>: <reason>, <path>: <reason>.`` (verbatim all-fail template).
4. `chflags nouchg "$SANDBOX"/*.md && rm -rf "$SANDBOX"` (cleanup).

**Three-outcome classification (per AC3 / epic L110–113):**
- **FULL PASS** = both scenarios emit the verbatim expected template. Risk 1 closes. CHANGELOG records FULL PASS.
- **PARTIAL PASS** = partial-success scenario passes (regression OK), all-fail still misfires (LLM regresses to partial-success container with `(none)` placeholder). Risk 1 does not close. CHANGELOG records PARTIAL PASS. Promote v0.1.9 candidate "programmatic mechanism for runtime LLM template adherence" to must-do.
- **FULL FAIL** = both scenarios regress. CHANGELOG records FULL FAIL. Same v0.1.9 promotion.

**This task does NOT block the pipeline regardless of outcome** — all three outcomes ship per E05 epic L110–113 (three-outcome ship policy). The task succeeds when both scenarios have been executed, transcripts captured, and an outcome classified. The classification feeds T6 (CHANGELOG entry).

**Platform note:** This is macOS (per env Darwin 24.6.0). Use `chflags uchg` (not `chmod 444` — Edit's write-temp + rename bypasses 444 per audit pitfall). Linux equivalent `sudo chattr +i` not used here.

**Verify:**
1. Both scenarios executed; transcripts captured.
2. Outcome classified per the three-outcome rule.
3. Sandbox cleaned up (`/tmp/doc-writer-t2-sandbox/` removed; no `chflags uchg` flags left on files).

**UI:** no

---

### T6: Create v0.1.8 CHANGELOG entry documenting AC1 anchoring + AC2 trim + AC3 T2 outcome + AC4 convention + AC5 trail artifacts (~5 min)

**Files:** `CHANGELOG.md`
**Depends on:** T1 (trim site names + word count for AC2 entry), T2 (E04 forward-pointer artifact), T3 (E05 forward-pointer artifact), T4 (CONTRIBUTING.md extension artifact), T5 (T2 transcripts + outcome classification for AC3 entry)
**Action:** Insert a new `## [0.1.8] — <today's date>` section above the current L3 `## [0.1.7]` line, with `### Changed` containing one combined entry covering all five AC items (AC1, AC2, AC3, AC4, AC5) per AC6.

**Details:**

The v0.1.7 entry's T2 PARTIAL PASS block (at CHANGELOG L53 per discovery) is the format reference for AC3's T2 transcript reporting.

New section structure:
```
## [0.1.8] — <YYYY-MM-DD>

> All-fail anchoring closure + cross-epic re-amendment convention extension. Scope per [docs/planning/epics/E06-anchoring-closure-and-ci-coverage.md](docs/planning/epics/E06-anchoring-closure-and-ci-coverage.md).

### Changed

- **doc-writer all-fail-branch anchoring tightened (E06.S1.AC1).** Three concurrent forms layered on `agents/doc-writer.md` per the E05 audit's three hypotheses:
  - (a) New standalone MUST sentence below the L41 universal MUST paragraph re-stating the branch-selection rule as its own discipline, paired with a new post-emit self-check below L57 ("confirm you selected the template matching your observed outcome").
  - (b) Own all-fail-specific MUST line above the all-fail code fence ("MUST literally begin with `doc-writer: all writes failed —`").
  - (c) All-fail-specific post-emit self-check below the AC1(a) self-check, naming the v0.1.7 misfire pattern by literal contrast.
- **Cap-relief trim (E06.S1.AC2).** Removed `## Writing Guidelines` section (L59–72, 35 words) and `## Rules` section (L74–79, 51 words) — combined 86 words — to absorb AC1's additions (~73 words). Net delta: `wc -w agents/doc-writer.md` = <N> (≤650 confirmed; pre-edit 649, post-edit <N>). Preserved coverage: E04.S8.AC1 (a)–(f) failure modes; E05.S2.AC2 three-form anchoring at L41/L48/L57 (byte-identical); E05.S2.AC3 `(no error output)` fallback at L55; E05.S2.AC4 all-fail template at L52 + branch-selection rule at L41 (both byte-identical).
- **Risk 1 (E05) T2 synthetic re-run executed against post-merge doc-writer.md (2026-06-03) — <CLASSIFICATION> per epic L110–113.** Sandbox at `/tmp/doc-writer-t2-sandbox/` with `pitfalls-stub.md` + `claude-stub.md`, writes blocked via `chflags uchg` (macOS — `chmod 444` bypassed by Edit's write-temp + rename per audit pitfall).
  - **Scenario 1 — partial-success (regression check):** <PASS/FAIL>. First line emitted: `<transcript-first-line>`.
  - **Scenario 2 — all-fail (primary):** <PASS/FAIL>. First line emitted: `<transcript-first-line>`.
  - Classification: <FULL PASS / PARTIAL PASS / FULL FAIL>. <Risk closure or v0.1.9 promotion language per outcome.>
- **Cross-epic AC re-amendment convention extended (E06.S1.AC4).** `CONTRIBUTING.md` `## Cross-epic AC amendments` subsection extended from single-hop (codified E05.S3) to multi-hop with a four-artifact requirement, recursive-application rule, and named first multi-hop instance (E04.S8.AC5 → E05.S2.AC4 → E06.S1). See `CONTRIBUTING.md` `## Cross-epic AC amendments`.
- **Re-amendment trail artifacts applied (E06.S1.AC5).** Forward-pointer added in `docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md` immediately below the preserved L452 `**Amended in E05.S2**` back-pointer (`**Re-amended in E06.S1 — see E06.S1 for the latest contract.**`); forward-pointer added in `docs/planning/epics/complete/E05-doc-writer-hardening-and-spec-quality-gates.md` immediately below E05.S2.AC4 prose at L117 (`**Re-amended in E06.S1 — see E06.S1 for the latest contract; this entry preserved as historical record.**`). E04.S8.AC5 prose at L451 and E05.S2.AC4 prose at L117 both preserved byte-identical; L442 and L448 back-pointers (E04.S8.AC2 and AC4 — not re-amended by E06.S1) preserved unchanged per AC5(a) scope-clarification.
```

**Implementer responsibility:**
- Substitute `<today's date>` with the date the implementation runs.
- Substitute `<N>` with the actual post-trim `wc -w agents/doc-writer.md` output.
- Substitute `<CLASSIFICATION>` with the T5 outcome (FULL PASS / PARTIAL PASS / FULL FAIL).
- Substitute `<PASS/FAIL>` for each scenario based on T5 verification.
- Substitute `<transcript-first-line>` with the verbatim first line of each scenario's captured transcript (in backticks).
- Choose the "Risk closure or v0.1.9 promotion language" based on the classification:
  - FULL PASS: "Risk 1 closes. v0.1.8 ships with Risk 1 closure documented."
  - PARTIAL PASS: "Risk 1 does not close. Promote v0.1.9 candidate 'programmatic mechanism for runtime LLM template adherence' to v0.1.9 must-do."
  - FULL FAIL: "Risk 1 does not close. Same v0.1.9 promotion."

**Verify:**
1. `grep -Fn "## [0.1.8]" CHANGELOG.md` returns 1 match positioned above the L3 `## [0.1.7]` line.
2. The v0.1.8 entry contains all 5 items (AC1 anchoring, AC2 trim with named word counts, AC3 T2 outcome with both transcripts, AC4 convention pointer, AC5 trail-artifact named files).
3. The v0.1.7 entry (currently at L3) is byte-identical to its pre-edit state (verify via `git diff` — only insertion above, no edits below).

**UI:** no

---

## Blast Radius

**Do NOT modify:**
- `agents/doc-writer.md` lines L1–4 (frontmatter), L20–34 (Process steps 1–5), L35 (Step 6 Deduplicate), L37–57 (entire Failure handling section EXCEPT additive insertions of AC1(a) MUST, AC1(b) MUST, AC1(a) self-check, AC1(c) self-check at specified positions). L41, L44, L48, L52, L55, L57 content all byte-identical per AC7.
- `docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md` L442 and L448 back-pointers (E04.S8.AC2 and AC4 — NOT re-amended by E06.S1 per AC5(a) scope-clarification). All AC prose above and below the L452 back-pointer.
- `docs/planning/epics/complete/E05-doc-writer-hardening-and-spec-quality-gates.md` L117 AC4 prose (preserved byte-identical per AC5(b) no-edit-in-place rule).
- `CONTRIBUTING.md` L1–76 (existing single-hop convention text, all preserved byte-identical) and L78+ (`## Tooling Pitfalls` and subsequent sections).
- `CHANGELOG.md` L3+ (existing v0.1.7 and prior entries preserved byte-identical).
- Any other plugin file not in the File Table.

**Watch for:**
- Word-budget arithmetic at intermediate states. T1 must trim before adding, else verify-all.sh could fail on intermediate `wc -w` overshoot (a hook may not actually run mid-task, but the discipline avoids the risk).
- Edit precision when inserting forward-pointers below existing back-pointers in E04 epic — the L442 and L448 back-pointers must NOT be matched. Use surrounding-context anchors unique to AC5 (e.g., AC5 prose tail).
- Line-number shifts after T1 trim — subsequent grep verifications should not assume original line numbers; verify by content match.
- CHANGELOG entry creation introduces a new top section — ensure the existing v0.1.7 entry is not accidentally modified.

## Conventions

- **ADR-001:** review-plan dispatched as blocking subagent. Stage 4 of build pipeline applies.
- **ADR-002:** fresh subagent per implementation task. Each of T1–T6 dispatched as its own fresh subagent in Stage 5.
- **ADR-003 / agents/agent-preamble.md:** doc-writer's failure-handling clauses live in `agents/doc-writer.md`, not in `agent-preamble.md` (per E04.S8 + E05.S2). E06.S1 preserves this placement (out-of-scope: lifting failure-handling clauses to preamble — epic L151).
- **ADR-006:** CLAUDE.md read at runtime by agents — `agents/doc-writer.md` Step 1 reads CLAUDE.md per project. T5's sandbox uses `claude-stub.md` to simulate a project CLAUDE.md.
- **AC7 byte-identical preservation** of L41, L48, L52, L55, L57 in `agents/doc-writer.md`: all content edits are positional insertions, not relocations or rewrites.
- **AC5(a) preserve-hops convention** in CONTRIBUTING.md L70–76 and the new multi-hop extension: every prior amender's pointers preserved; only the new latest-amender forward-pointer added at each hop.
- **AC2 trim-table 4-column requirement** is satisfied above (see Trim Table section).
- **CONTRIBUTING.md `## Cross-epic AC amendments` extension verbatim:** the AC4 convention text is tagged `verbatim:` in the epic (L121) — implementer must paste byte-for-byte modulo whitespace/link formatting.
- **CHANGELOG entry format:** mirror v0.1.7 (header + `### Changed` bulleted list). v0.1.7 T2 PARTIAL PASS entry at L53 is the format reference for the AC3 T2 transcript block.
