> **Status:** Historical — implemented and merged in commit f2d24f3f185f84f9104aaf9b5d22088b17dd2a1a on 2026-05-30. This plan was an active build/fix artifact; treat as historical reference only.

# Implementation Plan: E05.S5 — Stage 8 2-commit-window ABORT HANDLING entry

Plan-format-version: 1

## Summary

Add an ABORT HANDLING entry to `skills/shared/abort-handling.md` covering the window between commit 1 (feat:/fix: implementation) and commit 2 (`docs: mark <feature> plan historical`) of E04.S3's 2-commit Stage 8 pattern. Post-E05.S4 the shared file is the single authoritative source — no inline backport to build/fix SKILL.md. Update `CHANGELOG.md` `### Added`. Source spec: `docs/planning/epics/E05-doc-writer-hardening-and-spec-quality-gates.md` E05.S5 (L332–365).

## File Table

| File | Action | Task(s) |
|------|--------|---------|
| `skills/shared/abort-handling.md` | Modify (add new entry) | T1 |
| `CHANGELOG.md` | Modify (add ### Added entry) | T2 |

## Notes (resolved spec tensions)

**Tension A — AC1 header form vs AC2 awk regex (resolved via R-A).** AC1's verbatim entry text begins `**After Stage 8 step 3 commit, before step 4 commit (rare — no human gate exists in this window):**`, but AC2's structural-adjacency verify uses `awk '/^\*\*Stage 8/'` which matches only lines starting literally with `**Stage 8` (not `**After Stage 8`). Resolution: normalize the entry header to `**Stage 8 (after step 3 commit, before step 4 commit — rare: no human gate exists in this window):**`. This (i) matches the existing file's `**Stage[s] X (description):**` format byte-for-byte (AC1 "format matches existing entries byte-for-byte"), (ii) starts with `**Stage 8` so the AC2 awk anchor fires, (iii) preserves AC1's semantic content. AC1's "Entry text:" quote is treated as content specification rather than literal-byte header. The AC1 body prose (Recovery options (a)/(b) and trailing rationale sentence) is preserved verbatim.

**Tension B — recovery options (a)/(b) vs existing Stages 5-7 numbered 1/2/3 (preserved as AC1 specifies).** AC1's verbatim recovery options use lettered (a)/(b) inline-prose form. Stages 5-7 uses numbered 1/2/3 in a blockquote. The two forms are structurally distinct (inline prose vs blockquote) and serve different option-density needs (2 options vs 3 options). Preserving AC1's (a)/(b) inline form is consistent with AC1's explicit wording; the byte-for-byte clause governs header form, not recovery-options numbering style.

**Carve-out for review-plan AC5 (self-defeating verify pattern).** The new entry contains the literal string "2-commit window," and the AC2 supplementary `grep -Fc "2-commit window"` runs against the same file. Per `skills/review-plan/SKILL.md:45-46`, this triggers the self-defeating-verify check. **Carve-out applied:** AC2's PRIMARY verify is the awk **structural-position verify** with named anchor `/^\*\*Stage 8/`. This is explicitly named as a passing form in the L45-46 carve-out ("Use ... a structural-position verify ..."). The literal `grep -Fc` returning 1 is a redundant supplementary count-confirmation, not the primary verify — exactly matching the canonical positive pattern in the check's example.

**AC4 self-validation path.** This plan is itself reviewed by `/roughly:review-plan` at Stage 4 (post-E05.S3 ship). The plan body contains the explicit carve-out language above to ensure review-plan's AC5 self-defeating-verify check passes via the documented carve-out (structural-position verify named).

## Tasks

### T1: Add Stage 8 2-commit-window entry to `skills/shared/abort-handling.md` (~3 min)

**Files:** `skills/shared/abort-handling.md`

**Action:** Insert a new ABORT HANDLING entry covering the Stage 8 2-commit window, placed immediately after the existing Stages 5-7 block and before the existing "Always on abort" general entry.

**Details:**

Use `Edit` (not `Write`) with `replace_all: false`. The unique anchor is the literal "Always on abort:" header line.

- `old_string`:
  ```
  **Always on abort:** End with a clear message: "Pipeline aborted at Stage [N]. [cleanup summary]."
  ```
- `new_string`:
  ```
  **Stage 8 (after step 3 commit, before step 4 commit — rare: no human gate exists in this window):** Commit 1 already landed; do not revert. Recovery options: (a) manually run `git rev-parse HEAD` to capture `IMPL_SHA`, prepend the Status block per Stage 8 step 4 template, commit 2 (`docs: mark <feature> plan historical`); OR (b) accept the implementation-only commit and skip plan-historical marking — the plan stays as a Stage-3 snapshot per E05.S6's plan-implementation-drift framing. Recovery path (a) preserves the canonical 2-commit pattern; (b) accepts a 1-commit story as a documented exception.

  **Always on abort:** End with a clear message: "Pipeline aborted at Stage [N]. [cleanup summary]."
  ```

(The new entry is prepended above the existing "Always on abort:" line, with a blank line separating them — matching the file's inter-entry blank-line convention.)

The entry preserves AC1's verbatim "Entry text:" body. Header form is normalized per Tension A's R-A resolution above. The order Stage 8 → general ("Always on abort") matches the file's stages-then-general organizational principle.

**Verify:**

1. **Primary (structural-adjacency, AC2):** `awk '/^\*\*Stage 8/{stage8=NR} stage8 && /2-commit window/{print NR-stage8; exit}' skills/shared/abort-handling.md` must print a value ≤ 10 (proves the literal "2-commit window" appears within 10 lines of the `**Stage 8` anchor). Pre-edit baseline returns empty; post-edit must return a number ≤ 10. The literal "2-commit window" appears in the entry's inline prose `... — rare: no human gate exists in this window` — wait: that prose contains "in this window" not "2-commit window." See below.
2. **Supplementary literal count (AC2):** `grep -Fc "2-commit window" skills/shared/abort-handling.md` must return 1.

**⚠️ Verify-side check on Tension A's resolution:** The literal phrase "2-commit window" is referenced in AC2's awk pattern AND must appear in the new entry's prose for the awk to fire. **The header `**Stage 8 (after step 3 commit, before step 4 commit — rare: no human gate exists in this window):**` does NOT contain the literal "2-commit window."** The body text AC1 specifies also does NOT use "2-commit window" — it uses "this window," "2-commit pattern," "1-commit story." 

**Resolution for the verify:** add the literal phrase "2-commit window" once inside the entry body (semantic-equivalence preservation) so both the awk and the supplementary grep fire as AC2 specifies. Concrete edit: change the trailing rationale sentence from `Recovery path (a) preserves the canonical 2-commit pattern; (b) accepts a 1-commit story as a documented exception.` to `Recovery path (a) preserves the canonical 2-commit pattern (the 2-commit window's intended end-state); (b) accepts a 1-commit story as a documented exception.` This insertion is a minimal byte-level addition to AC1's body wording, preserves semantic content, and ensures both AC2 verify forms fire correctly. **Document this as a sub-byte-deviation from AC1's verbatim body text, justified by AC2's verify-command literal dependency.**

3. **Line count check (advisory):** `wc -l skills/shared/abort-handling.md` should increase by 2 (one blank line + one entry line; the blank line preceding `**Always on abort:**` already exists at L24 and is preserved). 25 → 27 lines expected. Non-authoritative — the structural-adjacency awk return at step 1 is the load-bearing verification.

**UI:** no

### T2: Add CHANGELOG ### Added entry for E05.S5 (~3 min)

**Files:** `CHANGELOG.md`

**Depends on:** T1 (so the CHANGELOG entry can reference the landed change accurately)

**Action:** Add a new bullet under `## [Unreleased]` → `### Added`, documenting the new 2-commit-window ABORT HANDLING entry with refs to E04.S3's surfacing of the gap and E04 epic L629.

**Details:**

Use `Edit` (not `Write`) with `replace_all: false`. Anchor on the last bullet of the current `### Added` section (the second pitfalls-capture bullet for E05.S3, which currently ends `... + the working precedent for future cross-cutting convention work.`). Append the new bullet immediately after.

The new bullet must:
- Lead with bold `**E05.S5 — Stage 8 2-commit-window ABORT HANDLING entry.**`
- Use the in-flight v0.1.7 entries' narrative-prose convention (E05.S3, E05.S4 precedent in same `## [Unreleased]` block)
- Use `[skills/shared/abort-handling.md](skills/shared/abort-handling.md)` link form
- Cite **E04.S3** as the originating surfacing context and **E04 epic L629** as the v0.1.7 candidate source
- Acknowledge the **additive-completeness-no-dogfood-exercise** posture per epic Risk 5 (line 31): the entry ships unexercised because no human gate exists in the window
- Mention **Tension A's resolution** (header form normalized to file convention) and the **sub-byte body addition** of "2-commit window" for AC2 verify dependency, so the PR description and future readers can reconstruct the spec-vs-implementation deviations

**Proposed bullet (final wording finalizes at edit time, but this is the structure):**

```
- **E05.S5 — Stage 8 2-commit-window ABORT HANDLING entry.** New entry in [skills/shared/abort-handling.md](skills/shared/abort-handling.md) covering the window between commit 1 (feat:/fix: implementation) and commit 2 (`docs: mark <feature> plan historical`) of E04.S3's 2-commit Stage 8 pattern. Entry placed immediately after the existing Stages 5-7 block and before "Always on abort" per the file's stages-then-general organizational principle. Header normalized to the file's existing `**Stage[s] X (description):**` form (`**Stage 8 (after step 3 commit, before step 4 commit — rare: no human gate exists in this window):**`) — deviation from the spec's verbatim "Entry text:" header to satisfy AC1's "format matches existing entries byte-for-byte" clause and AC2's awk anchor `/^\*\*Stage 8/`. One sub-byte body addition (`(the 2-commit window's intended end-state)` inserted in the trailing rationale sentence) preserves AC2's literal `grep -Fc "2-commit window"` dependency. Ships as additive completeness per epic Risk 5: no human gate exists in the 2-commit window, so the entry is not exercised by dogfood — correctness is structural (format byte-for-byte matches existing entries; placement matches file convention). Closes E04 epic L629 v0.1.7 candidate. Source: `docs/planning/epics/E05-doc-writer-hardening-and-spec-quality-gates.md` E05.S5.
```

**Verify:**

1. `grep -Fc "E05.S5 — Stage 8 2-commit-window ABORT HANDLING entry" CHANGELOG.md` returns 1.
2. `grep -Fc "L629" CHANGELOG.md` returns ≥ 2 (the existing pre-implementation reference at L50 plus the new E05.S5 entry).
3. New bullet appears under `## [Unreleased]` → `### Added` (not under `### Changed`) — visual inspection of `head -20 CHANGELOG.md` post-edit.

**UI:** no

## Blast Radius

- **Do NOT modify:**
  - `skills/build/SKILL.md` (inline backport prohibited per AC1's `Out of scope`; post-E05.S4 the shared file is authoritative)
  - `skills/fix/SKILL.md` (same)
  - The 2-commit Stage 8 pattern itself (E04.S3 locked; out of scope per epic L360)
  - Any other ABORT HANDLING entry (`Out of scope`: no other identified gaps)
  - `skills/shared/stage-8-wrap-up.md` (no Stage 8 procedural change)
  - `.claude/hooks/verify-all.sh` (Check 8 already covers the structural surfaces this story touches)
  - Any ADR (`docs/adrs/`): ADR-012 already covers the shared-reference pattern this story uses
  - CLAUDE.md, CONTRIBUTING.md, docs/adrs/README.md (no convention/structure changes)

- **Watch for:**
  - **verify-all.sh Check 8:** new entry must NOT introduce the load-bearing phrase `When the human selects "abort"` into build/fix SKILL.md (it doesn't — build/fix untouched). Must NOT remove this phrase from abort-handling.md (it stays at L5).
  - **Line-cap drift:** abort-handling.md has no cap; CHANGELOG.md has no cap; both unaffected.
  - **Bidirectional sync (E05.S4 reverse-verify gap, v0.1.8 candidate):** acceptable — Check 8 doesn't verify abort-handling.md content, only consumer-side directive presence. Risk 5 closure accepts this.
  - **Plan file lifecycle:** Stage 8 step 4 (post-implementation) will prepend a Status block to THIS plan file via the canonical 2-commit pattern. No special handling needed; the plan path `.roughly/plans/` exists.

## Conventions

- **ADR-012:** `skills/shared/abort-handling.md` is the authoritative source. Inline copies do NOT exist in build/fix SKILL.md (E05.S4 refactor confirmed). New abort-handling content lands ONLY in the shared file.
- **E04.S3:** 2-commit Stage 8 pattern (commit 1 = feat:/fix:; commit 2 = `docs: mark <feature> plan historical`). This story's wrap-up follows it.
- **E04.S6 / E05.S3 review-plan conventions:** every edit site enumerated as a separate task entry (T1, T2 above). Carve-out language for self-defeating-verify pattern documented in `## Notes` above and inline in T1's `Verify` block.
- **Risk 5 acceptance:** additive completeness; no dogfood exercise possible. PR description will document this explicitly.

## Stage 8 wrap-up reminders (post-implementation)

- Commit 1: `feat: add Stage 8 2-commit-window ABORT HANDLING entry (E05.S5)` per stage-8-wrap-up.md step 2 build branch.
- Commit 2: `docs: mark E05.S5-stage-8-2-commit-window-abort-handling plan historical` per stage-8-wrap-up.md step 4.
- Maturity checks: per stage-8-wrap-up.md step 5.
- Pitfalls capture: per stage-8-wrap-up.md step 6 — surface anything novel from this work (Tension A resolution pattern is a candidate).
