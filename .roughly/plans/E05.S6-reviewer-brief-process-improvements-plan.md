> **Status:** Historical — implemented and merged in commit fa28b77816e129380058a0bee594e2e5d13e23bd on 2026-05-31. This plan was an active build/fix artifact; treat as historical reference only.

# Implementation Plan: E05.S6 Reviewer-Brief Process Improvements

Plan-format-version: 1

Bundled per epic E05 §S6 — three prose-only process improvements surfaced from E04 cubic-iteration observation: epic-reviewer AC mutual satisfiability check (epic L597), plan-implementation drift framing at Stage 8 (L599), cubic-iteration termination criteria (L611). Self-validated by AC6 against the post-E05.S3 review-plan checks.

## File Table

| File | Action | Task(s) |
|------|--------|---------|
| `skills/shared/stage-8-wrap-up.md` | Edit | T1 |
| `agents/epic-reviewer.md` | Edit | T2 |
| `skills/review-plan/SKILL.md` | Edit | T3 |
| `skills/build/SKILL.md` | Edit | T4 |
| `skills/fix/SKILL.md` | Edit | T5 |
| `tests/fixtures/epic-reviewer/` (new dir) | Create | T6 |
| `tests/fixtures/epic-reviewer/README.md` | Create | T6 |
| `tests/fixtures/epic-reviewer/ac-mutual-satisfiability-pass.md` | Create | T6 |
| `tests/fixtures/epic-reviewer/ac-mutual-satisfiability-needs-revision.md` | Create | T6 |
| `tests/fixtures/review-plan/ac-joint-satisfiability-pass.md` | Create | T7 |
| `tests/fixtures/review-plan/ac-joint-satisfiability-needs-revision.md` | Create | T7 |
| `tests/fixtures/review-plan/README.md` | Edit | T7 |
| `CHANGELOG.md` | Edit | T8 |

## Tasks

### T1: AC4 — Drift framing in stage-8-wrap-up.md (~3 min)
**Files:** `skills/shared/stage-8-wrap-up.md`
**Action:** Add the plan-implementation drift framing note at the end of step 4, before step 5.
**Details:**
1. Read `skills/shared/stage-8-wrap-up.md` in full. Confirm step 4 is the "Plan historical marking" entry that ends with the literal sentence `no defensive \`mkdir -p\` is needed at this Edit site.` (verbatim per discovery; intentionally broader than enumeration not applicable — single file edit). Confirm step 5 is `5. Run maturity checks (see below).` on the next line.
2. Use the `Edit` tool with `replace_all: false`:
   - `old_string`: the verbatim two-line block consisting of (a) the closing of step 4 (`no defensive \`mkdir -p\` is needed at this Edit site.`) and (b) the next line (`5. Run maturity checks (see below).`)
   - `new_string`: the same two lines, with the framing prose inserted between them as a new indented continuation of step 4. The inserted text reads exactly:
     > The Status block frames the plan as a Stage-3 snapshot. Implementation actuals may differ from the plan — e.g., post-merge cubic-fix iterations that modify the shipped code without re-editing the plan — and that drift is expected, not a defect. Downstream review tools (cubic and similar) should treat the plan as historical context, not authoritative spec — the Status block + first-line marker signals this intent.
3. The insertion adds ~3 lines (one paragraph). Use a leading newline so the new paragraph is separated from step 4's body, and no trailing newline change before step 5.
**Verify:** `grep -Fc "drift is expected, not a defect" skills/shared/stage-8-wrap-up.md` returns exactly `1`. Also confirm `grep -c "^[0-9]\. " skills/shared/stage-8-wrap-up.md` returns the same count post-edit as pre-edit (no new numbered step accidentally introduced).
**UI:** no

### T2: AC1 — AC mutual satisfiability evaluation step in epic-reviewer.md (~5 min)
**Files:** `agents/epic-reviewer.md`
**Action:** Add a new evaluation step #7 to `## Review Dimensions`, appended after the existing dimension #6 (Dependencies).
**Details:**
1. Read `agents/epic-reviewer.md` in full to confirm current 72-line / 310-word baseline (cap 650 words; ample headroom).
2. Locate the `## Review Dimensions` block (lines ~25–32) and the final dimension entry, verbatim: `6. **Dependencies** — Are cross-story dependencies identified and correctly ordered?`
3. Use `Edit` with `replace_all: false`:
   - `old_string`: the full single line `6. **Dependencies** — Are cross-story dependencies identified and correctly ordered?`
   - `new_string`: the same line, followed by a newline, followed by the new dimension 7 entry. Dimension 7 text reads exactly:
     > 7. **AC mutual satisfiability** — For each pair of ACs that reference overlapping surfaces (same file path, same step number, same prose region, same fixture, or same constraint), verify joint satisfiability. If two ACs jointly create a structural impossibility — e.g., AC2 forbids modification outside step X AND AC1 requires the modification to fire when step X's gate is closed — flag as a blocker requiring AC amendment before the epic is approved. Canonical positive example: E04.S8's AC2/AC4-vs-AC1-reachability contradiction (would have been caught at epic-review iteration 2 instead of surfacing across multiple post-merge cubic iterations).
4. Match the existing single-line `N. **Bold label** — content` style; the example reference uses the identifier shorthand only (not prose-expanded), per discovery's "no precedent for inline examples" observation.
**Verify:** `grep -Fc "AC mutual satisfiability" agents/epic-reviewer.md` returns exactly `1` (matches AC1's verify command). Also confirm the file's word count remains under 650 — `wc -w agents/epic-reviewer.md` ≤ 650 (projected ~360).
**UI:** no

### T3: AC2 — Plan-level AC joint-satisfiability check in review-plan/SKILL.md (~5 min)
**Depends on:** None
**Files:** `skills/review-plan/SKILL.md`
**Action:** Insert a new check entry into the `**Assumptions:**` block — after the existing "Behavior-divergence doc coverage" entry and before the `**Overengineering:**` heading.
**Details:**
1. Read `skills/review-plan/SKILL.md` in full to confirm current 115-line baseline (cap 300; ample headroom).
2. Locate the verbatim Behavior-divergence carve-out closing line (line ~60), which ends with the canonical-negative sentence `Canonical negative: a new function that adds caching as its first call (no prior doc could describe the uncached path because the function is new).` followed by a blank line followed by the heading `**Overengineering:**` on the next non-blank line.
3. Use `Edit` with `replace_all: false`:
   - `old_string`: the verbatim block that contains the closing of the Behavior-divergence carve-out line, the blank line, and the `**Overengineering:**` heading
   - `new_string`: the same block, with the new check entry inserted between the blank line and `**Overengineering:**`. The new entry MUST match the E05.S3 entry style — bold-name top-line + trigger sentence + indented `- **Carve-out:**` sub-bullet with canonical positive/negative examples. The entry reads exactly:
     > - **AC joint satisfiability.** For each pair of ACs in the plan that reference overlapping surfaces (same file, same step, same prose region), verify joint satisfiability. If two ACs create a structural impossibility within the plan's implementation scope, flag for clarification before approving the plan.
     >   - **Carve-out:** ACs that reference orthogonal surfaces (different files, different steps, different prose regions) skip this check. Canonical positive: E04.S8's AC2/AC4-vs-AC1-reachability contradiction — AC2 forbade modification outside Stage 5 AND AC1 required the modification to fire when Stage 5's gate was closed; joint impossibility surfaced only post-merge during cubic iterations. Canonical negative: a plan whose ACs touch different files with no shared step or prose region — orthogonal, skip.
4. Match the surrounding entries' formatting exactly (top-level `- **Name.**`, indented `  - **Carve-out:**` sub-bullet). The entry adds ~3 lines (top-line + sub-bullet + blank line above next heading; sub-bullet may wrap).
**Verify:** `grep -Fc "joint satisfiability" skills/review-plan/SKILL.md` returns exactly `1` (AC2 verify; `grep -Fc` is safe here — only one entry is added on a distinct line, no same-line co-location risk because the new entry is a separately-bulleted check entry, **sites known to be on physically distinct lines**).
**UI:** no

### T4: AC3a — Cubic-iteration termination criteria in build/SKILL.md Stage 6 (~5 min)
**Files:** `skills/build/SKILL.md`
**Action:** Append a new paragraph to Stage 6 documenting the cubic-iteration termination rule, inserted between the existing "max 2 review-fix cycles" paragraph and the `**Gate:**` line.
**Details:**
1. Read `skills/build/SKILL.md` Stage 6 to confirm current ~266-line baseline and the verbatim insertion anchor: the paragraph ending `...most expensive loop in the pipeline, conversion-to-prompt deferred pending v0.1.5 dogfood evidence).` followed by a blank line followed by `**Gate:** "Review complete. Proceed to verification?...`
2. Use `Edit` with `replace_all: false`:
   - `old_string`: the verbatim two-paragraph block — the closing line of the review-fix paragraph (`...conversion-to-prompt deferred pending v0.1.5 dogfood evidence).`), a blank line, and the `**Gate:**` line opening.
   - `new_string`: the same two paragraphs with the new termination-criteria paragraph inserted between them. The new paragraph reads exactly:
     > Cubic iterations (post-merge re-runs of `/roughly:review` or an external cubic tool) terminate when: (a) cubic returns `{"issues": []}` (clean), OR (b) 5 iterations have completed with progressively narrower findings (diminishing-returns observable: each iteration surfaces fewer or lower-severity issues than the prior), OR (c) the remaining finding can only be addressed by spec amendment — escalate as a candidate via the active epic's v0.1.X candidates section, then accept the current state as documented-deferral.
3. Use a leading blank line before and after the new paragraph so it sits as its own block. Adds ~5 lines (including blank-line separators).
**Verify:** `grep -Fc "Cubic iterations" skills/build/SKILL.md` returns exactly `1`. Also `wc -l skills/build/SKILL.md` ≤ 300 (projected ~271).
**UI:** no

### T5: AC3b — Cubic-iteration termination criteria in fix/SKILL.md Stage 6 (~3 min)
**Depends on:** T4 (copy the same paragraph for byte-identity)
**Files:** `skills/fix/SKILL.md`
**Action:** Insert the same termination-criteria paragraph in fix/SKILL.md Stage 6 at the analogous location (between max-cycles paragraph and `**Gate:**`).
**Details:**
1. Read `skills/fix/SKILL.md` Stage 6 to confirm current ~267-line baseline and the verbatim insertion anchor — analogous to build/SKILL.md Stage 6's anchor.
2. Use `Edit` with `replace_all: false` to insert the byte-identical paragraph from T4 between the same two anchor blocks.
3. The inserted paragraph MUST be byte-identical to the version inserted in T4 (this is structural-uniformity prose — the cubic-termination rule is the same rule in both pipelines; rationale matches the ABORT HANDLING + Stage 8 shared-reference pattern but without the off-ramp since the prose is ≤5 lines per file).
**Verify:** `grep -Fc "Cubic iterations" skills/fix/SKILL.md` returns exactly `1`. Also `wc -l skills/fix/SKILL.md` ≤ 300 (projected ~272 — confirms epic's L248 arithmetic). Then run `diff <(awk '/^Cubic iterations/,/documented-deferral\./' skills/build/SKILL.md) <(awk '/^Cubic iterations/,/documented-deferral\./' skills/fix/SKILL.md)` — must emit zero diff (byte-identical paragraph in both files).
**UI:** no

### T6: Epic-reviewer synthetic fixtures (AC1 verification) (~5 min)
**Files:**
- `tests/fixtures/epic-reviewer/README.md` (new)
- `tests/fixtures/epic-reviewer/ac-mutual-satisfiability-pass.md` (new)
- `tests/fixtures/epic-reviewer/ac-mutual-satisfiability-needs-revision.md` (new)

**Action:** Create the new `tests/fixtures/epic-reviewer/` directory with a README and a PASS + NEEDS REVISION fixture pair exercising the epic-level AC mutual satisfiability check (AC1).
**Details:**
1. Create the directory: `mkdir -p tests/fixtures/epic-reviewer/` (defensive — must exist before Write calls).
2. Write `README.md` following the `tests/fixtures/review-plan/README.md` structural convention: a brief purpose section, a fixture inventory table (`Fixture | Verdict | Tests`), and a "How to Verify" section explaining how to dispatch the epic-reviewer agent against each fixture and what to expect.
3. Write `ac-mutual-satisfiability-pass.md`: a minimal synthetic epic doc with two ACs that reference orthogonal surfaces (different files, no overlap). Required outcome when dispatched to epic-reviewer: Ready / no blockers (AC1's check does not fire because the surfaces are orthogonal — matches the AC2 carve-out's "different files, different steps, different prose regions" rule).
4. Write `ac-mutual-satisfiability-needs-revision.md`: a minimal synthetic epic doc with two ACs that mirror the E04.S8 contradiction pattern — AC2 forbids modification outside step X, AND AC1 requires the modification to fire when step X's gate is closed. Required outcome when dispatched to epic-reviewer: blocker verdict citing "AC mutual satisfiability" by name.
5. Both fixtures should be ≤30 lines each — minimal enough to exercise the check cleanly without unrelated noise.
**Verify:** `ls tests/fixtures/epic-reviewer/` lists exactly 3 files (README.md + 2 fixtures). `grep -Fc "AC mutual satisfiability" tests/fixtures/epic-reviewer/ac-mutual-satisfiability-needs-revision.md` returns `≥1` (fixture must contain the trigger pattern it's testing). Live dispatch validation deferred to Stage 6/7 — the orchestrator will dispatch the epic-reviewer agent against both fixtures and confirm the expected verdicts before commit.
**UI:** no

### T7: Plan-level review-plan fixtures (AC2 verification) (~5 min)
**Files:**
- `tests/fixtures/review-plan/ac-joint-satisfiability-pass.md` (new)
- `tests/fixtures/review-plan/ac-joint-satisfiability-needs-revision.md` (new)
- `tests/fixtures/review-plan/README.md` (edit)

**Action:** Create the PASS + NEEDS REVISION fixture pair exercising AC2 (plan-level joint satisfiability) under the existing `tests/fixtures/review-plan/` directory; extend the fixtures README inventory.
**Details:**
1. Confirm `tests/fixtures/review-plan/` exists with 22 fixtures + README (per discovery). No `mkdir` needed.
2. Write `ac-joint-satisfiability-pass.md`: minimal synthetic plan with two ACs referencing orthogonal surfaces (different files / different steps / different prose regions). Required outcome when dispatched to review-plan: PASS (joint-satisfiability check does not fire — carve-out applies).
3. Write `ac-joint-satisfiability-needs-revision.md`: minimal synthetic plan with two ACs that touch the same file + same step + same prose region with a structural impossibility (e.g., AC1 requires adding a line; AC2 forbids modifying that file's line count beyond ±0). Required outcome when dispatched to review-plan: NEEDS REVISION citing AC2 / joint satisfiability by name.
4. Edit `tests/fixtures/review-plan/README.md` to add the two new fixtures to the inventory table, matching the existing row format byte-for-byte. Adds 2 inventory rows.
5. Both fixtures should be ≤30 lines each.
**Verify:** `ls tests/fixtures/review-plan/ | wc -l` returns `25` (22 prior fixtures + README + 2 new). `grep -Fc "joint satisfiability" tests/fixtures/review-plan/ac-joint-satisfiability-needs-revision.md` returns `≥1`. README inventory shows the two new rows. Live dispatch validation deferred to Stage 6/7.
**UI:** no

### T8: AC5 — CHANGELOG `### Added` entries (~3 min)
**Depends on:** T1–T7 (CHANGELOG written after all other edits land so file paths and line counts are stable)
**Files:** `CHANGELOG.md`
**Action:** Add three distinct bullets under the `## [Unreleased]` / `### Added` block, one per process improvement, with cross-references to the surfacing E04 candidate epic line numbers (L597, L599, L611).
**Details:**
1. Read `CHANGELOG.md` top — confirm `## [Unreleased]` block at line 1 with an `### Added` sub-block containing the prior E05 entries (E05.S2, E05.S3, E05.S4, E05.S4.5, E05.S5 should already be present).
2. Use `Edit` with `replace_all: false`. Identify the most recent existing E05 bullet's closing line as the anchor; append the three new bullets immediately after it, matching the E05.S5 style — `- **E05.S6 — [title].** [narrative with file path link and surfacing reference].`
3. The three bullets MUST be distinct (per AC5 explicit requirement — "three distinct improvements, not bundled into one bullet"):
   - Bullet 1: epic-reviewer AC mutual satisfiability check (new dimension 7 in `agents/epic-reviewer.md`; surfacing reference epic L597)
   - Bullet 2: plan-implementation drift framing at Stage 8 (new prose in `skills/shared/stage-8-wrap-up.md` step 4; surfacing reference epic L599)
   - Bullet 3: cubic-iteration termination criteria in Stage 6 (new prose in `skills/build/SKILL.md` + `skills/fix/SKILL.md`; surfacing reference epic L611)
4. Each bullet references its file path(s) via markdown links matching the existing CHANGELOG convention. The AC2 plan-level review-plan addition can be absorbed into bullet 1 OR mentioned in its own sub-bullet under bullet 1 — implementer's choice based on entry length; AC5 specifies three distinct improvements (epic-reviewer dimension, drift framing, cubic-termination), not four.
**Verify:**
- `grep -Fc "E05.S6" CHANGELOG.md` returns `≥3` (three bullets, each prefixed with the story ID).
- `grep -Fc "AC mutual satisfiability" CHANGELOG.md` returns `≥1`.
- `grep -Fc "plan-implementation drift" CHANGELOG.md` returns `≥1`.
- `grep -Fc "cubic-iteration termination" CHANGELOG.md` returns `≥1`.
**UI:** no

## Blast Radius

- **Do NOT modify:**
  - The pre-flight block (lines containing `<!-- pre-flight:start -->`) in any skill — byte-identity enforced by `verify-all.sh` Check 1
  - The `## ABORT HANDLING` section head in `skills/build/SKILL.md` or `skills/fix/SKILL.md` — `verify-all.sh` Check 8 anchor
  - The `## STAGE 8: WRAP-UP` section head in `skills/build/SKILL.md` or `skills/fix/SKILL.md` — `verify-all.sh` Check 8 anchor
  - The `Read \`skills/shared/stage-8-wrap-up.md\`` directive line(s) in build/fix SKILL.md — line-start enforced
  - The E05.S5 Stage 8 entry in `skills/shared/abort-handling.md` — out of E05.S6 scope
  - Any existing E04.S6, E05.S3 fixture in `tests/fixtures/review-plan/`
  - Any existing review dimension #1–6 in `agents/epic-reviewer.md` — append only
  - Any existing E05.S3 check entry in `skills/review-plan/SKILL.md` — append only
- **Watch for:**
  - `verify-all.sh` Check 8 fires only on consumer drift (build/fix SKILL.md inline-duplicates of shared-file load-bearing phrases). Editing `skills/shared/stage-8-wrap-up.md` content (T1) does NOT trigger the drift check — the check is consumer-facing only. Confirmed by discovery against `.claude/hooks/verify-all.sh` Check 8 logic.
  - Line-cap budget post-edit (cap 300 lines on build/fix SKILL.md): build 266+~5=~271, fix 267+~5=~272. Both clean under cap. review-plan 115+~3=~118 (cap 300). Stage-8-wrap-up has no line cap.
  - Word-cap budget on `agents/epic-reviewer.md` (cap 650 words): 310+~50=~360. ~290 words headroom.
  - Self-defeating verify risk: T2's verify command (`grep -Fc "AC mutual satisfiability" agents/epic-reviewer.md`) operates on a scope known to be free of the literal — `agents/epic-reviewer.md` does not contain this plan's narrative prose, only the new evaluation step that we are intentionally placing in it. T3's verify (`grep -Fc "joint satisfiability"`) similarly operates only on `skills/review-plan/SKILL.md`. T4/T5's verify (`grep -Fc "Cubic iterations"`) operates only on the build/fix SKILL.md files. T1's verify on `drift is expected, not a defect` operates only on `skills/shared/stage-8-wrap-up.md`. None of these scopes contains this plan file's narrative prose — **all verify commands operate on a scope known to be free of the literal outside the intentional insertion target**.
  - Same-line co-location: every T1–T5 verify uses `grep -Fc` against a single-occurrence target appearing on its own line (new check entries, new dimensions, new paragraphs) — **sites known to be on physically distinct lines** per the AC entry being a standalone bulleted/paragraph addition.

## Conventions

- E05.S3 review-plan entry style: bold-name + trigger sentence + indented `- **Carve-out:**` with canonical positive/negative examples (review-plan/SKILL.md L36/L39/L42/L45 pattern). T3 matches this byte-for-byte.
- Epic-reviewer Review Dimension style: `N. **Bold label** — single question or evaluation form` (epic-reviewer.md L27–32 pattern). T2 matches.
- Stage 6 prose style: plain paragraph, no gate/bold heading prefix (build/fix SKILL.md L203/L208 pattern). T4/T5 match.
- CHANGELOG `### Added` style: `- **E05.SN — [title].** [narrative]` (E05.S5 entry pattern in CHANGELOG.md). T8 matches.
- Fixture directory README style: brief purpose + inventory table + "How to Verify" section (`tests/fixtures/review-plan/README.md` pattern). T6 README matches.
- Shared-reference pattern: ADR-012. Edits to `skills/shared/stage-8-wrap-up.md` content (T1) do not require corresponding edits to build/fix SKILL.md — the runtime `Read` directive resolves the content fresh per invocation.

## AC6 Self-Validation Carve-Out Phrases

This plan is the first instance of E05.S6.AC6's "review-plan reviews its own AC additions" exercise. The plan body intentionally contains language that maps to the actual `skills/review-plan/SKILL.md` carve-out phrases (NOT the epic's literal-quoted forms — see Discovery Risk 1 — three of five epic-enumerated phrases differ from the actual SKILL.md text):

- **For E05.S3.AC1 (verify-vs-enumeration asymmetry):** N/A — this plan's AC verify commands all operate on the enumerated single-file scope; no asymmetry to acknowledge.
- **For E05.S3.AC2 (`grep -Fc` co-location):** acknowledged in Blast Radius with the phrase `sites known to be on physically distinct lines` — matches `skills/review-plan/SKILL.md` line 43 verbatim.
- **For E05.S3.AC3 (defensive guard vs invariant):** N/A — this plan adds no defensive guards; all changes are pure additive prose.
- **For E05.S3.AC4 (behavior-divergence doc coverage):** N/A — no new guards layered onto previously-reachable code paths; all changes are greenfield additions (the term `greenfield addition` is the actual SKILL.md L60 phrase if any reviewer's check fires here).
- **For E05.S3.AC5 (self-defeating verify):** acknowledged in Blast Radius with the phrase `verify commands operate on a scope known to be free of the literal` — matches `skills/review-plan/SKILL.md` line 46 verbatim ("operating on a scope known to be free of the literal").

If `/roughly:review-plan` returns NEEDS REVISION on any of E05.S3.AC1–AC5 against this plan, the matching carve-out language above is the AC6-acceptable acknowledgment. If review-plan flags an AC NOT covered above, this plan must be revised to add the carve-out before Stage 4 PASS (per AC6's enumerated-list-only rule).
