# Changelog

## [Unreleased]

### Added

- **Review-plan spec-quality gates — 5 new check entries (E05.S3).** Added 5 new check entries to `skills/review-plan/SKILL.md`: (1) AC verify-command scope vs spec enumeration (asymmetry detection with "intentionally broader than enumeration" carve-out — Completeness dimension; closes E04 epic L607, L609); (2) `grep -Fc` / `grep -Fn` same-line co-location hazard (Completeness; closes E04 epic L621); (3) defensive guard vs new invariant distinction (Assumptions; closes E04 epic L617); (4) behavior-divergence doc coverage (guards before previously-reachable code must audit existing docs — Assumptions; closes E04 epic L625); (5) self-defeating verify pattern (literal-form verify against scope containing the new prose — Completeness; closes E04 epic L609 reinforced by audit S1.AC5 finding). Each new check follows the E04.S6 template: bold name + trigger + bright-line carve-out + canonical positive/negative example pair. Source: `docs/planning/epics/E05-doc-writer-hardening-and-spec-quality-gates.md` E05.S3.
- **Stronger output-mandatory wording in `skills/review-plan/SKILL.md` (E05.S3 AC6a).** Added "Every dispatch MUST produce a verdict block" language at the top of `## Output Format` and as a new bullet in `## Rules`. Surfaced 2026-05-15 during E04.S6 Stage 4 dispatch where the agent stopped mid-investigation; closes E04 epic L583 candidate.
- **CONTRIBUTING.md `## Skill authoring conventions` extended (E05.S3 AC6b).** Added a new convention: README/doc invocation examples must align with skill invocation surface. Pipeline coordinator skills (`build`, `fix`) have `disable-model-invocation: true` but ARE user-invocable via slash command — the field prevents conversational invocation, not slash-command dispatch. Pure subagent-dispatch-only skills (`review-plan`, `review`, `review-epic`, `audit-epic`, `verify-all`) have no slash-command surface and must NOT be documented as user-invocable. Closes E04 epic L581 (folded into CONTRIBUTING.md convention rather than review-plan AC per OQ10).
- **CONTRIBUTING.md `## Cross-epic AC amendments` subsection (E05.S3 AC6c).** New top-level subsection codifying the 3-artifact convention for amending an already-shipped story's AC from a later epic: (1) amending epic entry is canonical source; (2) original epic entry gains back-pointer note in the form `**Amended in <new-story-id> — see <new-story-id> for the corrected contract.**`; (3) CHANGELOG `### Changed` entry cross-references both. Codified here per E05 epic Risk 4 mitigation (codify before first use to avoid chicken-and-egg of E05.S2 being simultaneously the first amendment and the codifier). First instance: E05.S2 amends E04.S8 AC2/AC4/AC5.
- **15 new test fixtures + README update under `tests/fixtures/review-plan/` (E05.S3 AC7c).** 5 fixture triples (PASS + NEEDS REVISION + BORDERLINE-PASS) — one triple per new AC1–AC5: `ac1-broader-scope-*.md`, `ac2-grep-fc-*.md`, `ac3-defensive-guard-*.md`, `ac4-doc-coverage-*.md`, `ac5-self-defeating-*.md`. **BORDERLINE-PASS is a new fixture form** introduced in this story to protect against false-positive review-fatigue (E05 epic Risk 3) — exercises the carve-out boundary by presenting a legitimate-but-unusual plan structure requiring the reviewer to recognize substantive rationale, not just literal phrase matching. Existing 7 E04.S6 fixtures retained unchanged. `tests/fixtures/review-plan/README.md` updated with: header mentioning E05.S3 and the new BORDERLINE-PASS form, a one-paragraph note defining the three fixture forms, 15 new rows in the fixture inventory table (5 triples), and Path A desk-check line-number references refreshed for all 7 checks (existing E04.S6 AC1 at L36, AC2 at L53; new E05.S3 AC1 at L39, AC2 at L42, AC3 at L56, AC4 at L59, AC5 at L45).
- **Two pitfalls captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E05.S3** (new `## Review-Plan Fixture Design` section): **(1) PASS-fixture-must-fire-trigger** — a PASS fixture for a review-plan AC must contain the trigger condition the AC fires on (e.g., AC1's broader-scope verify must have a verify command broader than its enumerated file list). Fixtures lacking the trigger don't exercise the carve-out; they exercise nothing. Surfaced during E05.S3 fixture triple authoring. **(2) BORDERLINE-PASS-single-acknowledgment** — a BORDERLINE-PASS fixture must include exactly one carve-out acknowledgment (not zero, not multiple); zero acknowledgments degrades to NEEDS REVISION semantically, and multiple acknowledgments dilute the single-rationale signal the form is designed to test. Together these capture the discipline behind the new BORDERLINE-PASS form introduced by E05.S3 AC7c.
- **E05.S5 — Stage 8 2-commit-window ABORT HANDLING entry.** New entry in [skills/shared/abort-handling.md](skills/shared/abort-handling.md) covering the window between commit 1 (feat:/fix: implementation) and commit 2 (`docs: mark <feature> plan historical`) of E04.S3's 2-commit Stage 8 pattern. Entry placed immediately after the existing Stages 5-7 block and before "Always on abort" per the file's stages-then-general organizational principle. Header normalized to the file's existing `**Stage[s] X (description):**` form (`**Stage 8 (after step 3 commit, before step 4 commit — rare: no human gate exists in this window):**`) — deviation from the spec's verbatim "Entry text:" header to satisfy AC1's "format matches existing entries byte-for-byte" clause and AC2's awk anchor `/^\*\*Stage 8/`. One sub-byte body addition (`(the 2-commit window's intended end-state)` inserted in the trailing rationale sentence) preserves AC2's literal `grep -Fc "2-commit window"` dependency. Ships as additive completeness per epic Risk 5: no human gate exists in the 2-commit window, so the entry is not exercised by dogfood — correctness is structural (format byte-for-byte matches existing entries; placement matches file convention). Closes E04 epic L629 v0.1.7 candidate. Source: `docs/planning/epics/E05-doc-writer-hardening-and-spec-quality-gates.md` E05.S5.
- **Two pitfalls captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E05.S5** (commit `c5b6288`; file 140 → 144 lines): **(1) Verbatim-text ACs vs regex-anchor verify ACs against the same artifact can be mutually unsatisfiable** — the target file's existing format convention is the resolving third constraint. Origin: E05.S5 Tension A (AC1's spec-verbatim header form starting with "After Stage 8" was structurally incompatible with AC2's awk anchor matching lines beginning with "Stage 8"); resolution normalized the entry header to the file's existing `**Stage[s] X (description):**` form, treating AC1's "Entry text:" preamble as content specification rather than literal-byte header. Reinforces the v0.1.8 candidate "AC quoted-wording marker convention" already surfaced from E05.S2 (candidate #5). **(2) Triage step required before reclassifying silent-failure-hunter findings against shared-reference files** — first-draft pitfall wording was overbroad and could have suppressed legitimate defects (cubic P2 flagged). Rewrote to require verification that the canonical-prose reference both exists AND contains the flagged detail before reclassifying an SFH finding as design-tradeoff vs. real defect. Origin: E05.S5 Stage 6 SFH triage against ADR-012 shared-reference target files.
- **Eight v0.1.8 candidates surfaced from E05.S5** (silent-failure-hunter findings reclassified as design-tradeoff observations per Risk 5 byte-for-byte mitigation + ADR-012 single-source-of-truth; recorded for v0.1.8 PM round): (1) **Path (a) procedural-detail vs canonical-pointer tradeoff** — path (a) omits halt-on-failure, substitution guard, and old_string sourcing (all canonical in `skills/shared/stage-8-wrap-up.md:28` step 4). Inline embedding would violate Risk 5 + create drift surface. Candidate: evaluate whether shared-reference abort entries should embed an explicit "re-read canonical file step N" hint for LLM during abort recovery vs. relying on implicit pointer inference. (2) **Path (b) documented-exception trail location** — AC1's path (b) accepts a 1-commit story as a documented exception but names no artifact (PR description? CHANGELOG? follow-up issue?). Candidate: add a single phrase to AC1 naming the documentation location. (3) **Observable trigger condition for the entry** — header says "rare — no human gate exists" but doesn't describe the actual trigger (Ctrl+C between commits? hook failure on commit 2? session timeout?). LLM may misclassify into Stages 5–7. Candidate: add an observable-signal trigger description (e.g., commit 1 visible in git log but plan's first line does NOT begin with the Status block prefix). Relates to existing pitfall "LLM-interpreted conditionals must reference observable signals." (4) **Stages 5–7 destructive-option exclusion in Stage 8 context** — partial mitigation present ("Commit 1 already landed; do not revert"). Candidate: add explicit "the Stages 5–7 rollback options do not apply once commit 1 has landed" to close the fall-through path. (5) **Angle-bracket placeholder metasyntax in agent-facing prose** — pre-existing pattern in `skills/shared/abort-handling.md` and `skills/shared/stage-8-wrap-up.md`; not new from S5. Cross-cutting candidate: scan for unmarked angle-bracket placeholders across agent-facing prose; reinforce with explicit "substituting the actual feature name" hedging where they appear. (6) **AC4 PASS verdict persistent-artifact convention** — Stage 4 review-plan PASS evidence currently lives only in session logs. Candidate: convention for recording the PASS verdict in the plan file or CHANGELOG for audit trail. (7) **`.roughly/known-pitfalls.md` size organization sweep** — file at 144 lines vs 80 threshold; Stop hook Check 3 advising. Already candidate #4 from E05.S2; E05.S5 raises the file from 140 to 144. (8) **AC2 awk degenerate-return-value semantics** — AC2 awk returns 0 when header and "2-commit window" land on the same single-line paragraph (header NR equals phrase NR). Numerically passes the bound but is structurally indistinguishable from "phrase appears in header itself." Candidate: AC verify-command authoring hygiene for single-line markdown entries with header-internal anchors that degenerate AC2-style adjacency verifies.
- **Risk 5 (Stage 8 additive completeness) closes by-design at E05.S5 ship** per epic L31 mitigation. Entry ships as additive completeness; no human gate exists in the 2-commit window so no dogfood exercise is possible. Correctness is structural — format byte-for-byte matches existing entries; placement matches file convention. E05.S3's every-edit-site-enumerated review-plan check (generalized form per E04.S6 origin) satisfied via Stage 4 review-plan PASS (1 iteration, 0 blockers).
- **E05.S6 — Epic-reviewer AC mutual satisfiability check.** New Review Dimension #7 in [agents/epic-reviewer.md](agents/epic-reviewer.md) flags ACs that jointly create structural impossibilities (overlapping surfaces: same file path, same step number, same prose region, same fixture, or same constraint). Mirrored at plan-review level by a new "AC joint satisfiability" check entry in [skills/review-plan/SKILL.md](skills/review-plan/SKILL.md) Assumptions block. Canonical example: E04.S8's AC2/AC4-vs-AC1-reachability contradiction (would have been caught at epic-review iteration 2 instead of surfacing across multiple post-merge cubic iterations). Surfaced from E04 candidates L597.
- **E05.S6 — Plan-implementation drift framing at Stage 8.** New framing prose in [skills/shared/stage-8-wrap-up.md](skills/shared/stage-8-wrap-up.md) step 4 explicitly frames historical-marked plans as Stage-3 snapshots; post-merge cubic-fix iterations that modify shipped code without re-editing the plan are expected drift, not a defect. Downstream review tools (cubic and similar) should treat the plan as historical context, not authoritative spec — the Status block + first-line marker signals this intent. Surfaced from E04 candidates L599.
- **E05.S6 — Cubic-iteration termination criteria in Stage 6.** New byte-identical paragraph in [skills/build/SKILL.md](skills/build/SKILL.md) Stage 6 and [skills/fix/SKILL.md](skills/fix/SKILL.md) Stage 6 documents three termination conditions for post-merge cubic re-runs: (a) cubic returns clean (`{"issues": []}`), (b) 5 iterations with progressively narrower findings (diminishing returns), or (c) remaining finding requires spec amendment — escalated as a candidate via the active epic's v0.1.X section, then accepted as documented-deferral. Surfaced from E04 candidates L611.
- **One pitfall captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E05.S6** (commit `04f63e8`; file 144 → 146 lines): **`grep -c '^[0-9]\. '` cross-section over-matching** — `grep -c` against a numbered-list anchor matches numbered prefixes ACROSS THE ENTIRE FILE, not the target section. Plans using absolute expected counts silently mis-baseline when other sections in the same file also use numbered lists. Pattern: scope the count via `awk '/start-marker/,/end-marker/' | grep -c` first, OR use a delta assertion (expected-post minus expected-pre equals N) rather than an absolute count. Reusable verify-command authoring hygiene for any plan with cross-section numbered-list operations.
- **One v0.1.8 candidate surfaced from E05.S6** (recorded for v0.1.8 PM round): **Runtime enforcement of cubic-termination evidence-artifact contracts** — E05.S6.AC3 introduced two honor-system contracts: option (b) requires a per-iteration summary (issue count + max severity) and option (c) requires a diff to the active epic's v0.1.X candidates section as the escalation artifact. Both currently rely on implementer discipline; not enforced by `.claude/hooks/verify-all.sh` (epic L425 explicitly scopes runtime enforcement out). v0.1.8 promotion candidate IF dogfood evidence shows the contracts being honored-in-breach (e.g., cubic option-b iterations landing without summary entries; option-c escalations missing the diff). Don't promote without evidence.
- **Risk 2 (off-ramp shared-reference drift) — first AC4-edit exercise of the shared-file invariant.** E05.S6.AC4 modified `skills/shared/stage-8-wrap-up.md` (the post-E05.S4 authoritative source) for the first time post-off-ramp. Check 8 confirmed consumer-facing drift detection only — no false-positive triggered on the modification. 30-day window continues per by-design closure contract; closes opportunistically at v0.1.8 retrospective on zero false-positive accumulation. ~24 days remaining in window (opened 2026-05-28 at E05.S4 ship).
- **Risk 3 (review-plan AC false-positive flagging) — first production-availability validation via AC6 self-validation.** E05.S6's plan was reviewed by post-E05.S3 `/roughly:review-plan` with the 5 new checks active; returned PASS in 1 iteration. The plan body's "Carve-Out Phrases" section pre-emptively addressed AC4 (greenfield) and AC5 (scope free of literal) — review-plan returned PASS without needing the carve-out section to invoke, but the defensive documentation was load-bearing for the PASS verdict. Risk 3 mitigation validated for this plan shape; closes-on-ship-with-fixture-triples per epic Risk 3 close criterion **fully satisfied** at E05.S6 ship.

### Changed

- **E05.S2 — doc-writer multi-file failure-handling contract revision.** Relocates the failure-handling clause from `agents/doc-writer.md` Process step 5 into a new `## Failure handling` top-level section. Strengthens the partial-success template anchoring (MUST-language imperative, code-fenced template, post-emit self-check), adds an empty-error fallback, and adds an all-fail branch template with branch-selection rule. Amends E04.S8 AC2/AC4/AC5 (original entries in `docs/planning/epics/complete/E04-path-consolidation-and-process-codification.md` gain back-pointer notes; corrected contract is the E05.S2 entry of `docs/planning/epics/E05-doc-writer-hardening-and-spec-quality-gates.md`). Convention codified in `CONTRIBUTING.md` `## Cross-epic AC amendments`.

- **Three pitfalls captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E05.S2** (commit `04f50e2`, refined post-fix `8d6f754`): **(1) LLM self-check anchor pattern** — prefer `begins with <literal-prefix>` + literal prefix over `is <text> — …` + ellipsis. The ellipsis is illustrative metasyntax, not a wildcard; an `is` anchor with ellipsis is silently unverifiable by the LLM doing post-emit self-validation. Cross-cutting pattern for any agent doing post-emit self-check; not doc-writer-specific. **(2) Failure-handling sections need scoping in the opening sentence, not just the heading** — applies to any agent with branched output templates conditioned on failure presence. Without explicit opening-sentence scoping, the runtime LLM falls back to free-form prose on the un-templated case (e.g., all-success multi-file dispatch). The three-way-pick fix pattern (all-success / partial-success / all-fail templates with explicit branch-selection rule) is the recommended remediation. **(3) Codifier and first-applier stories for cross-cutting conventions need explicit sequencing** — codification + first application in one story creates a self-referential AC. E05.S3 ↔ E05.S2 worked as a clean codified-pair pattern: S3 codifies the `## Cross-epic AC amendments` convention in CONTRIBUTING.md; S2 applies it to amend E04.S8 ACs. Captures the anti-pattern + the working precedent for future cross-cutting convention work.

- **Six v0.1.8 candidates surfaced from E05.S2** (recorded for v0.1.8 PM round): (1) **Risk 1 T2 runtime classification** — re-run synthetic T2 test with post-merge anchoring in place; record outcome (FULL PASS / PARTIAL / FULL FAIL per E05.S2 AC2); update epic accordingly. Documentation task, low risk. (2) **Other-agents branched-output-template scoping audit** — investigator, code-reviewer, silent-failure-hunter, static-analysis, epic-reviewer currently emit no branched templates, but the three-way scoping pattern from pitfall #2 should apply from the start if any future agent gains a `## Failure handling` section. (3) **Other-agents self-check sentences `is + ellipsis` pattern audit** — quick grep across `agents/` for self-check anchors using the deprecated "first line is X — …" form; replace with the "first line begins with X" form. Likely zero hits today; cheap to verify. (4) **`.roughly/known-pitfalls.md` organization sweep** — file at 138 lines vs 80 threshold; Stop hook Check 3 advising. Batch with other doc-organization candidates. (5) **AC quoted-wording marker convention (spec-authoring)** — when an AC quotes example wording containing metasyntactic notation (angle-bracketed placeholders, ellipses, XXX), explicitly mark whether the wording is verbatim-required or illustrative-only. Suggested convention: `verbatim: TEXT` vs `form: TEXT (adapt for clarity)`. Would have prevented both E05.S2 post-merge deferrals (AC2(c) anchoring wording + AC4 binary-vs-three-way rule). Strong candidate for E05.S3 augmentation (new `/roughly:review-plan` AC) or CONTRIBUTING.md skill-authoring conventions extension. (6) **Stage 6 SFH third-disposition gate** — today's Stage 6 gate has `fix` vs `defer`. A third option — `spec-revision candidate, not a code defect` — would have surfaced both E05.S2 deferrals correctly at build-time. Touches build/fix Stage 6 prose (stayed inline post-E04.S4 off-ramp per OQ11; not in shared file).

- **Risk 1 (LLM weak-anchoring) partial close from E05.S2.** Three-form anchoring landed (MUST imperative + code-fenced template + post-emit self-check); post-merge fix `8d6f754` strengthened anchoring beyond spec form (closes the `is + ellipsis` self-check unverifiability + adds three-way branch-selection rule covering the all-success case). Synthetic T2 runtime test **deliberately deferred to v0.1.8** per E05 epic L113 ("do not block the release on full PASS"); landed as candidate #1 above. Risk 1 closes opportunistically at v0.1.8 retrospective on T2 PASS.

- **E05.S4 — Off-ramp refactor: ABORT HANDLING + Stage 8 prose extracted to `skills/shared/`.** Two new authoritative procedural references created: [skills/shared/abort-handling.md](skills/shared/abort-handling.md) (byte-identical copy of the previously-inline ABORT HANDLING prose) and [skills/shared/stage-8-wrap-up.md](skills/shared/stage-8-wrap-up.md) (parametric merge of build + fix Stage 8 with inline pipeline-conditional prose at the 2 divergence sites: step 2 commit-message stanza `feat:`/`fix:` and step 6 wording `this work`/`this fix`). Consumer SKILL.md files refactored to 3-line shared-reference directives at each section head: [skills/build/SKILL.md](skills/build/SKILL.md) 299 → 266 lines; [skills/fix/SKILL.md](skills/fix/SKILL.md) 300 → 267 lines — closes the 300/300 binding state that constrained any v0.1.6+ fix-touching work, leaving ~33-line headroom. New [ADR-012](docs/adrs/ADR-012-runtime-shared-procedural-references.md) codifies the runtime-shared-reference pattern (distinct from ADR-003's copy-and-sync sync-reference pattern). New Check 8 added to [.claude/hooks/verify-all.sh](.claude/hooks/verify-all.sh) (114 → 148 lines) detects four drift modes: missing `skills/shared/` directory, missing shared file, missing section heading or missing Read directive at line-start within 3 lines of the heading in a consumer (proximity per ADR-012 / AC5 — uses `grep -A 3` to extract the 4-line window then `awk 'index($0, p) == 1'` for fixed-string line-start match; immune to both regex-metacharacter bypass like `Read \`skills/shared/abort-handlingXmd\`` and prefix-corruption like `XX Read \`...\`` which a `grep -qE "^Read \`...\`"` check would silently pass), and inline duplication of a load-bearing phrase in a consumer (`When the human selects "abort"` for abort-handling, `` 1. `git add` changed files `` for stage-8-wrap-up). [CLAUDE.md](CLAUDE.md) Structure table gains a `skills/shared/<name>.md` row; ADR enumeration updated `ADR-009, ADR-011` → `ADR-009, ADR-011, ADR-012`; Key Design Decisions table gains ADR-012 row. [CONTRIBUTING.md](CONTRIBUTING.md) `## Skill authoring conventions` extended with a Runtime-shared procedural references convention paragraph (when to extract vs inline, drift-check enforcement, distinction from ADR-003). [docs/adrs/README.md](docs/adrs/README.md) Current ADRs list adds ADR-012. Source: `docs/planning/epics/E05-doc-writer-hardening-and-spec-quality-gates.md` E05.S4.

- **One pitfall captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E05.S4** (cubic iteration 1 finding): **Bash literal-directive line-start checks** — both `grep -qE` and `grep -qF` are unsound for verifying that a fixed-string directive lives at line-start in a markdown file. `grep -qE` is bypassed by regex-metacharacter substitution in the haystack (a `.` in the pattern matches any byte in a corrupted file); `grep -qF` is bypassed by prefix-corruption (e.g., garbage characters before the expected line-start — the `-F` flag doesn't anchor). Sound mechanic: `grep -A 3` to extract the line-window around the section heading, then `awk 'index($0, p) == 1'` for fixed-string line-start match. Reusable convention for any future drift-check authoring in `.claude/hooks/verify-all.sh`.

- **Two v0.1.8 candidates surfaced from E05.S4** (deferred-NOTE patterns from Stage 6 review; recorded for v0.1.8 PM round): (1) **Reverse-verify gap on Check 8** — the hook confirms SKILL.md ↔ shared-file directive presence but does not reverse-verify that shared files still contain their load-bearing phrases. Bidirectional-sync obligation documented in hook comment + CHANGELOG. Candidate for hardening if Risk 2 dogfood surfaces a real drift case during the 30-day window. (2) **Stage 8 shared-file cross-pipeline ambiguity** — `skills/shared/stage-8-wrap-up.md` step 5 says "Run maturity checks (see below)"; works for build/fix consumers (both have MATURITY CHECKS in-place) but the cross-file reference becomes ambiguous if a future `/roughly:hotfix` (or any new pipeline skill) invokes the same shared file. AC1 byte-identity requirement on step 5 made this a deferred concern; surface when a new pipeline skill emerges.

- **Risk 2 (off-ramp shared-reference drift) enters its 30-day dogfood window** per the by-design closure contract (E05 epic Risk 2). Window opened at S4 ship (2026-05-28); closes opportunistically at v0.1.8 retrospective on zero false-positive accumulation from Check 8 on `main`.

- **E05.S1 — Agent word cap raised 500 → 650 project-wide.** The cap constant moved in [.claude/hooks/verify-all.sh](.claude/hooks/verify-all.sh) at two lines: line 27 (the comment documenting the threshold) and line 30 (the single-line conditional plus drift-message format string `- $f: $n words exceeds <N> cap`); only the numeric value swaps — the format string itself is preserved verbatim (AC2). [CONTRIBUTING.md](CONTRIBUTING.md) was updated in three normative locations — line 39 (PR Process), line 121 (Testing section), and line 166 (Check 3 under `## Stop hook drift checks`) — plus a one-line rationale paragraph appended to the `## Stop hook drift checks` section immediately before `## License`. [CLAUDE.md](CLAUDE.md) line 39 (`Agent prompts must stay under 650 words`) was updated as the project's own runtime-read convention statement. Compliance: `agents/doc-writer.md` is now compliant at **557/650** pre-E05.S2 (previously 557/500 — an accepted Path B violation from E04.S8 that closes structurally with this revision); projected **595–625** post-E05.S2, preserving ≥25 words of headroom at the upper projection. Rationale: accommodates E05.S2's failure-handling clauses (verbatim summary templates, all-fail branch, empty-error fallback) and provides ≥50 words of headroom at the projected post-E05.S2 state rather than landing at the edge of a tighter cap. Scope note: the original E05.S1 spec named 3 files; discovery surfaced normative `500` references in CONTRIBUTING.md lines 39 and 121 (outside the `## Stop hook drift checks` section the spec named) and CLAUDE.md line 39, which would have created internal contradiction if left at 500; scope expanded to 4 files with user approval, and AC5 amended in the implementation plan to enumerate the 4 expected files in `git diff --stat`.

- **`mkdir -p` audit (E05.S4.5):** Added explicit parent-guaranteed rationale at `skills/shared/stage-8-wrap-up.md` (Stage 8 plan-historical Edit). One audit site — `agents/doc-writer.md` `.roughly/known-pitfalls.md` Edit — could not accept an inline rationale without breaching the 650-word agent cap (file at 649/650 pre-edit); the rationale is recorded in the PR description audit report as a documented carve-out. Closes E04 epic L613 audit candidate.

- **One pitfall captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E05.S4.5** (commit `d1abcae`): **Near-cap agent-file carve-out for audit work** — when an audit story's AC mandates an in-place rationale addition but the target file is at its word cap (e.g., `agents/doc-writer.md` at 649/650 post-E05.S2), the inline rationale cannot land without breaching the cap or modifying surrounding prose (which violates no-prose-mod constraints). Documented carve-out pattern: record the rationale in the PR description audit report + CHANGELOG bullet + a generalized known-pitfalls entry; preserve the safety reasoning by an independent mechanism (in E05.S4.5 the doc-writer site uses `Edit`, which fails loudly on missing files — the silent-failure mode `mkdir -p` exists to guard does not apply). Reusable convention for future audit/audit-fix stories hitting cap walls.

- **Two v0.1.8 candidates surfaced from E05.S4.5** (recorded for v0.1.8 PM round): (1) **Setup Step 5b/5e defensive-mkdir consistency** — Setup relies on upstream Step 5 head's directory creation rather than defensive `mkdir -p` at each Write site, inconsistent with Step 5d's defensive pattern. Currently safe; cosmetic consistency gap. (2) **Stage 8 step 6 doc-writer dispatch-side escalation on "all writes failed" returns** — dispatch site doesn't specify orchestrator behavior on `doc-writer: all writes failed — …` returns. Pre-existing concern surfaced by silent-failure-hunter during S4.5 review (pre-dates E05.S2's all-fail template addition, since pre-S2 there was no all-fail template to read). Downstream of E05.S2's template work; defer to v0.1.8 (E05.S6 already carries 4 distinct concerns; expanding scope would increase blast radius).

- **Risk 1 (E05) T2 synthetic test executed during epic audit (2026-05-31) — PARTIAL PASS classification per epic L110–113.** Sandboxed dispatch of `roughly:doc-writer` against two stand-in files in `/tmp/doc-writer-t2-sandbox/` (writes blocked via `chflags uchg` for deterministic OS-level Edit failure, ruling out the chmod-444 bypass that the Edit tool's write-temp + rename pattern silently no-ops through). **Partial-success scenario (1 Edit succeeds, 1 fails): FULL PASS.** Runtime LLM emitted the verbatim partial-success template as the **first line** of its return: `doc-writer: partial success — wrote to: /tmp/doc-writer-t2-sandbox/pitfalls-stub.md; failed to write: /tmp/doc-writer-t2-sandbox/claude-stub.md: EPERM: operation not permitted, open '/tmp/doc-writer-t2-sandbox/claude-stub.md'.` — `<path>: <reason from Edit error output>` formatting per AC3 held; the AC2 three-form anchoring (MUST imperative at agents/doc-writer.md:41 + code-fenced template at L48 + post-emit self-check at L57) held; no pre-template prose. **All-fail scenario (0 succeeded / 2 failed): FAIL.** Runtime LLM regressed to the partial-success template with a contrived `(none)` placeholder for the successful-paths slot (`doc-writer: partial success — wrote to: (none); failed to write: /tmp/doc-writer-t2-sandbox/pitfalls-stub.md: EPERM …, /tmp/doc-writer-t2-sandbox/claude-stub.md: EPERM …`) instead of picking the AC4 all-fail template at `agents/doc-writer.md:52` (`doc-writer: all writes failed — <list>`). The branch-selection rule at L41 (`Pick template by outcome: 0 failed → all-success; ≥1 failed and ≥1 succeeded → partial-success; 0 succeeded → all-fail`) was not honored at the runtime LLM layer for the 0-succeeded case — the LLM kept the partial-success container and filled `wrote to:` with `(none)` rather than switching templates. **Risk 1 does NOT close**; per epic L112 PARTIAL PASS rule, the partial-success anchoring is the documented working pattern; the all-fail branch surfaces as v0.1.8 must-do. **v0.1.8 candidate #1 from L31 ("Risk 1 T2 runtime classification — documentation task") is promoted** to: **AC4 all-fail-branch anchoring tightening + T2 re-run**. Candidate hypotheses for the tightening: (a) lift the branch-selection rule from inline-with-MUST-sentence at L41 to its own MUST-imperative + post-emit self-check pair (mirror the L57 self-check structure for branch selection); (b) apply three-form reinforcement to the L52 all-fail template currently relying on the L41 MUST shared with partial-success (e.g., its own MUST-language line above the code fence); (c) add a separate all-fail-specific post-emit self-check ("Before returning, if you observed 0 successful writes, confirm your first line begins with `doc-writer: all writes failed —`, NOT `doc-writer: partial success —`"). Test artifacts (full transcripts of both dispatches) recorded in [docs/planning/epics/E05-doc-writer-hardening-and-spec-quality-gates-audit.md](docs/planning/epics/E05-doc-writer-hardening-and-spec-quality-gates-audit.md) "Post-audit follow-up" section.

- **E05 epic audit completed (2026-05-31).** Post-implementation audit of all 7 E05 stories via `/roughly:audit-epic`; report at [docs/planning/epics/E05-doc-writer-hardening-and-spec-quality-gates-audit.md](docs/planning/epics/E05-doc-writer-hardening-and-spec-quality-gates-audit.md). Verdict: **43 MET / 2 PARTIALLY MET / 0 NOT MET** across 45 ACs. Partial findings are documentation-grade and behaviorally inconsequential: (1) **E05.S1 AC5** — commit `432b778` `--stat` includes the `.roughly/plans/E05-S1-…-plan.md` build pipeline artifact not enumerated in the amended-AC5 4-file list (artifact is a pipeline byproduct; spirit of AC5 — no agent file edits — preserved). (2) **E05.S4.5 AC3** — the audit table lives in the plan appendix + commit body of `b883677` but PR #54's GitHub body contains only the cubic auto-summary, not the verbatim table; the L45/L47 convention names the rationale as "recorded in the PR description audit report" but discoverability via GH PR body is missing. Risk closure per epic DoD: Risk 4 (cross-epic AC amendment record) **CLOSED**; Risk 5 (2-commit-window abort handling) **CLOSED**; Risk 3 (review-plan AC false-positive flagging) **OPPORTUNISTIC CLOSE** validated at E05.S6 ship; Risk 1 (LLM template-adherence) **OPEN by accepted policy** — T2 deferred to v0.1.8 per candidate #1 of E05.S2's bundle; Risk 2 (shared-reference drift) **30-day window in progress** ~21 days remaining. Two v0.1.8 candidates surfaced by the audit (additive to prior bundles; recorded for v0.1.8 PM round): (i) **`.claude/hooks/verify-all.sh` near-cap (148/150)** — Check 8 from E05.S4 landed verify-all.sh at the 150-line soft cap with 2-line headroom; next drift-check addition either breaches the soft cap or requires the same kind of off-ramp work E05.S4 did for build/fix SKILL.md. Candidate: cap-pressure assessment + extraction plan (e.g., per-check helper functions in a sourced `.sh` library, or per-check separate hook files invoked from a top-level dispatcher) when a forcing-function story emerges. (ii) **Audit-table-in-PR-body convention** — the L47 pitfall + L45 CHANGELOG bullet both say "rationale recorded in the PR description audit report" but PR #54's GH body has only the cubic auto-summary; the table is recoverable from the plan appendix + commit body, so discoverability is degraded, not lost. Candidate: codify in `CONTRIBUTING.md` `## Audit conventions` (or extend `## Skill authoring conventions` if no audit-specific section exists) that audit stories MUST paste the full audit table into the GH PR body at PR-creation time, separate from any plan-appendix copy. Defer to v0.1.8 unless a second instance of the divergence arises sooner. Source: `docs/planning/epics/E05-doc-writer-hardening-and-spec-quality-gates-audit.md`.

## [0.1.6] — 2026-05-24

> Path consolidation + process codification. Scope per [docs/planning/epics/E04-path-consolidation-and-process-codification.md](docs/planning/epics/E04-path-consolidation-and-process-codification.md).

### Added

- **E04.S3 — Plan-file self-marking historical at completion.** Generalizes the S8 mitigation (Status block prepended to the S8 plan post-implementation to prevent cubic from treating the plan as actionable instructions) into a build/fix Stage 8 step that fires on every successful completion. Stage 8 (WRAP-UP) split into a **2-commit pattern**: commit 1 = implementation (feat); commit 2 = Status block prepend referencing commit 1's SHA (docs: mark `<feature>` plan historical). Block format is fully specified: `> **Status:** Historical — implemented and merged in commit <SHA> on <YYYY-MM-DD>. This plan was an active build/fix artifact; treat as historical reference only.` Insertion via `Edit` (not `Write`) per the append-only pitfall: `old_string` = plan's first line; `new_string` = Status block + blank + first line. Explicit halt-on-failure for empty SHA, Edit no-match, commit hook rejection; deterministic content, no approval gate. New byte-identical Stage 8 step 4 in [skills/build/SKILL.md](skills/build/SKILL.md) L236 (298 → 299) and [skills/fix/SKILL.md](skills/fix/SKILL.md) L239 (299 → 300 **AT CAP**); off-ramp NOT invoked — substitution-margin fit. New `## Plan-file lifecycle` subsection in [CONTRIBUTING.md](CONTRIBUTING.md) at L89 (24 lines; above original 10–15 spec'd, expanded to include a "Verifying the sweep" note documenting the `grep -L` false-positive surfaced by cycle-1 review). New ci-dogfood Assertion 5 at [scripts/ci-dogfood.sh](scripts/ci-dogfood.sh) L201 (`head -1 "$PLAN_FILE" | grep -qE '^> \*\*Status:\*\* Historical'`); structural-assertion count 6 → 7. **34 historical plans retro-marked** via per-file `Edit` sweep (33 pre-S3 + S3's own via the new Stage 8 step 4 — bootstrap of the 2-commit pattern). Merged 2026-05-23 via PR #47 (`26774cd`, 3 commits — `013cb7a` feat (38 files, +429/-5), `f964c81` S3 plan historical-mark bootstrap, `a859d82` `grep -L` false-positive pitfall capture). **Risk 6 (cubic-gate format rejection) closes** — branch / commit 1 / commit 2 all returned `{"issues": []}` on first cubic iteration; no format adjustments needed. **Cycle 1 caught 2 Criticals + 3 Warnings (all addressed in cycle 2 to 0/0); cycle 2 clean.** Spec corrections at plan-write: "Stage 7" → Stage 8 (Stage 7 is VERIFY, WRAP-UP is Stage 8); "25 historical plans" → 33 at branch start (+S3's own = 34); AC1 "wrap-up commit's parent" was self-contradictory against single-commit Stage 8, resolved via 2-commit interpretation; AC5 `grep -L` verify command unsound, replaced with `head -1 | grep -qE` first-line check. **Line-cap budget end-state: fix at 300/300 binding** — any future fix-touching story must use substitution-only or invoke the off-ramp (extract MATURITY CHECKS or ABORT HANDLING block to a shared reference).

- **One pitfall captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E04.S3 cycle-1 review** (commit `a859d82`): **`grep -L` is unsound for documentation sweeps where files may quote the pattern they search for.** `grep -L <pattern>` returns files where NO line matches — meaning it returns "unmarked" for any file where at least one line does NOT match (almost always true). The intended semantic (files that entirely lack the pattern) breaks down when a doc file's prose quotes the exact pattern string. Concrete origin: E04.S3 AC5 used `grep -L "^> \*\*Status:\*\* Historical" .roughly/plans/*.md` to find unmarked plans; the E04.S3 plan itself matched at L124 (where the plan's prose described the Status block format for CONTRIBUTING.md), causing the sweep to report the plan as already marked before Stage 8 ran. The ci-dogfood Assertion 5 carried the same any-line-vs-first-line bug. Fix: for first-line-only markers, use `head -1 "$f" | grep -qE 'pattern'` inside a loop — immune to quoted-pattern false positives in the body.

- **One v0.1.7 candidate surfaced from E04.S3** (recorded in [E04 epic v0.1.7 candidates](docs/planning/epics/E04-path-consolidation-and-process-codification.md#v017-candidates)): **ABORT HANDLING extension for Stage 8's 2-commit window.** The existing ABORT HANDLING block covers Stages 1–7 explicitly but has no entry for "after step 3 commit, before step 4 commit." Practical impact low (no human gate exists in that window so abort can't realistically trigger) but the gap is real. Blocker for v0.1.6: `skills/fix/SKILL.md` at 300/300; adding even one line would breach the cap. v0.1.7 work: pair the off-ramp invocation (extract MATURITY CHECKS or ABORT HANDLING to shared reference) with the 2-commit-window ABORT HANDLING entry as a single story.

- **E04.S9 — CI dogfood polish: macOS `gtimeout` + `ANTHROPIC_API_KEY` empty-guard.** Two small additive patches to [scripts/ci-dogfood.sh](scripts/ci-dogfood.sh). **(1) `$TIMEOUT` detection block** at L13–21: `command -v timeout` (Linux/CI), fallback to `command -v gtimeout` (macOS via coreutils), else friendly diagnostic exit (`install coreutils on macOS via 'brew install coreutils'`). Replaces three literal `timeout` invocations at L74/L100/L140 with `$TIMEOUT`. **(2) `ANTHROPIC_API_KEY` empty-guard** at L23–27: `${ANTHROPIC_API_KEY:-}` form is load-bearing for correctness (`set -euo pipefail` is already active at L2; `[ -z "$VAR" ]` on an unset variable would kill the script before the guard message). The workflow's auth-failure step bypasses the script entirely (invokes `claude` directly with `invalid-key-xyz`); the empty-guard is unreachable from that step. [CONTRIBUTING.md](CONTRIBUTING.md) `## CI` section gains a one-line gtimeout note at L134; L109 reworded to document the new empty-guard behavior post-cubic-round-2 (the empty-guard IS a replacement for the previously-reachable `claude --bare`-mediated auth-failure path when the key is unset — not additive prose as initially planned). Merged 2026-05-22 via PR #46 (`f3ff1ed`, 2 commits — `cc388f1` main feat + `8d953d1` L109 doc-coverage fix). Clean pipeline: PASS first plan-review cycle, 0 critical/warning at Stage 6 (2 LOW notes both spec-locked), cubic rounds 1 and 3 clean; round 2 caught 1 P2 (L109 doc/behavior contradiction). **AC2 spec correction:** initial spec said the `${VAR:-}` form was "future-proofing for `set -u` if ever added"; actually `set -u` is already active and the form is required for present-state correctness.

- **One v0.1.7 candidate surfaced from E04.S9** (recorded in [E04 epic v0.1.7 candidates](docs/planning/epics/E04-path-consolidation-and-process-codification.md#v017-candidates)): **additive-vs-replacement misclassification at plan-write** — when a guard or early-exit is added before previously-reachable code, any documentation describing the previously-reachable behavior via that input path must be examined for accuracy, not classified as "additive prose untouched." S9's plan carved out CONTRIBUTING.md L109 on this reasoning; both Stage 6 reviewers flagged a LOW-confidence doc/behavior mismatch but neither escalated; cubic round 2 caught it as P2. **Forms a coherent review-plan-as-spec-quality-gate v0.1.7 cluster** with E04.S1's AC-verify-scope-vs-enumeration, E04.S2's `grep -Fc` co-location, E04.S5's defensive-guard-vs-new-invariant, and E04.S6's every-edit-site-enumerated checks — recommend landing as a single coherent v0.1.7 story.

- **E04.S2 — Marker-aware resume reporting in `/roughly:upgrade`.** Adds explicit resume-step reporting and a shared abort-suffix idiom to [skills/upgrade/SKILL.md](skills/upgrade/SKILL.md) (172/300 lines unchanged — all edits in-place). Two new resume-report emits fire when `.migration-in-progress` is detected: `"Resuming v0.1.4 migration from step 5 of 10 (marker dated YYYY-MM-DD; steps 1-4 already ran)."` at L26 (v0.1.4 step 2 resume branch) and `"Resuming v0.1.6 migration from step 2 of 3 (marker dated YYYY-MM-DD; step 1 already ran)."` at L62 (new v0.1.6 step 1 resume-detection branch). Both instructions explicitly read the first line of the marker to extract the ISO date for substitution (observable-signal grounding per `.roughly/known-pitfalls.md` L52). Three new abort-suffix landings of the shared idiom `"Marker preserved at <path> for resume on next /roughly:upgrade."`: v0.1.4 step 5 mv-non-zero and data-loss aborts (both on L44 — sites 1 and 2 share the physical line because the two abort arms are adjacent in step 5's paragraph) and v0.1.6 step 2 mv-non-zero abort (L64). v0.1.6 step 2 (L64) also gains a `Skip the marker write if mode = resume` prefix to preserve the original migration date — parallels v0.1.4 step 3's existing skip-if-resume idiom. **Scope decisions:** v0.1.2 vacuously satisfies AC1 — its migration step has no `.migration-in-progress` marker and no numbered sub-steps, so the AC's trigger condition is structurally impossible (the AC1 spec line labels v0.1.2's destination `.ruckus/`; the actual L19 target is `.roughly/` — spec typo, doesn't affect the conclusion). AC5 ("existing completion-summary unchanged") vacuously satisfied — discovery confirmed none of the three migrations emit a completion-summary line today, so there is nothing to disturb. Marker-write-failure aborts (v0.1.4 L34, mid-L64 in v0.1.6 step 2) deliberately do NOT receive the suffix — the marker is not on disk at those abort points; "marker preserved" would be a false claim. **Pipeline note:** Stage 4 plan-review cycle 1 caught two `grep -Fc` verify commands that miscount when two abort sites land on the same physical line (the AC3 same-line case); fixed cycle 2 by switching to `grep -Fo ... | wc -l` for occurrence counts. Stage 6 review applied one disambiguation: `Otherwise check git status` → `Then check git status` at L62 to clarify the post-resume-branch flow (the pre-existing `Otherwise` originally negated the skip-if-absent check; after the resume-branch insertion the binding became ambiguous).

- **E04.S5 — Stop hook drift coverage expansion.** Three new structural-invariant checks added to [.claude/hooks/verify-all.sh](.claude/hooks/verify-all.sh) (57 → 114 lines, soft cap 150): **(1) pre-flight wording byte-identity** across the 7 hard-abort skills (audit-epic, build, fix, review, review-plan, review-epic, verify-all) against canonical fixture [tests/fixtures/canonical-preflight-block.txt](tests/fixtures/canonical-preflight-block.txt) — setup's soft-abort form explicitly excluded per the existing E03.S4 known-pitfall; **(2) `plan-mode-gate` hook-pair** presence + byte-identity (`.claude/hooks/plan-mode-gate.sh` ↔ `skills/setup/templates/plan-mode-gate.sh.template`) — scope broadened mid-review from byte-identity-only to a three-level cascade (template missing → hook missing → drift detected) per ADR-009 silent-protection-unregistration risk; **(3) `.roughly/known-pitfalls.md` `wc -l > PITFALLS_ORGANIZE_THRESHOLD=80`** with **bidirectional sync comments** naming `agents/doc-writer.md` Process step 5 ("Organize suggestion") and back — closes the E03.S3 manual-edit coverage gap. New `## Stop hook drift checks` section in [CONTRIBUTING.md](CONTRIBUTING.md) enumerating all 7 checks (4 pre-existing + 3 new) with the AC7-required E03.S2 dogfood-vs-template divergence cited by section heading + quoted phrase. **Cross-platform mechanic:** `shasum` (default on macOS + full Linux) with `sha1sum` fallback (BusyBox/Alpine minimal containers without Perl); `command -v` detection chain at script setup; replaced initial `md5sum` mechanic which is not portable to stock macOS. **Defensive precondition guards** layered during review: fixture-existence guard for Check 1, per-skill marker-presence pre-check producing directed `pre-flight markers missing in skills: <list>` diagnostic, tooling-unavailable branch when both `shasum` and `sha1sum` are absent, three-level presence cascade for Check 2 — all surface catastrophic-state failures rather than silently collapsing to false-PASS. **Cross-platform fix surfaced at implementation:** macOS BSD `wc -l <` emits left-padded output (`"      90"`) that breaks AC3.2's verbatim format `"is 90 lines"`; resolved via `$((n))` arithmetic expansion in the drift message — strips padding cross-platform. Merged 2026-05-20 via PR #44 (`bd8e37c`, 6 commits — main feat + 1 plan record + 1 pitfalls record + 3 cubic review-fix iterations). **Risk 3 (Stop hook drift-coverage expansion silently misfires) enters its 30-day dogfood window** per the by-design closure contract; closes at v0.1.7 retrospective on zero false-positive accumulation evidence.

- **Three pitfalls captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E04.S5** (+18 lines net at capture, 90 → 108; file intentionally triggers Check 3 on every run as designed): (1) **`md5sum` is not portable to stock macOS** — use `shasum` (default on macOS + full Linux) with `sha1sum` fallback (BusyBox/Alpine); detect via `command -v` chain. (2) **Empty-hash collapse in `extract | hash | sort -u` byte-identity pipelines** — when all inputs are missing or produce empty strings, the pipeline silently collapses to `unique=1` and reports PASS for a workspace-broken state. Fix: layer precondition guards (fixture-existence + per-file marker-presence) and a tooling-unavailable branch with directed diagnostics. Surfaced as Critical by silent-failure-hunter at Stage 6 review. (3) **`wc -l <` BSD padding leaks into format-equality tests** — macOS `wc -l <file` emits `"      90"` (left-padded) where GNU emits `"90"`. Naive `$n` interpolation into a format string breaks verbatim-format equality across platforms. Fix: `$((n))` arithmetic expansion in the format string strips padding cross-platform without external tooling.

- **One process observation surfaced as v0.1.7 candidate during E04.S5** (recorded in [E04 epic v0.1.7 candidates](docs/planning/epics/E04-path-consolidation-and-process-codification.md#v017-candidates)): **AC8 "no new invariants" boundary erosion under iterative defensive review** — S5's AC8 forbade adding invariants beyond the three named, but two rounds of review (Stage 6 + cubic round 4) pushed defensive precondition guards into what could be read as existence enforcement. Both fixes preserved AC8 in spirit but the boundary was relitigated twice in the same story without an explicit rule. v0.1.7 candidate: codify the "defensive guard vs new invariant" distinction as a `/roughly:review-plan` AC, paired with E04.S6's "every edit site enumerated" check.

- **E04.S8 — doc-writer multi-file-invocation guard.** Closes the v0.1.5 audit's S3 AC2 quality finding ("LLM agent conditionals need explicit failure-handling clauses"). New "Multi-file failure handling" sub-bullet appended to [agents/doc-writer.md](agents/doc-writer.md) Process step 5 at L35: per-file independent `Edit` invocation, per-file outcome capture, complete-what-you-can semantics (do NOT roll back successful writes on failure), AC5-verbatim partial-success summary template (`doc-writer: partial success — wrote to: <list>; failed to write: <list with reasons>`), never claim full success when any write partially failed. Sub-bullet heading carries an inline gate-override prefix `(always — overrides step 5's outer gate)` as a best-effort hedge — strict AC2/AC4 forbid placing the clause outside step 5, but step 5's outer gate is success-conditional on the file that may be FAILING in the target scenario; the inline prefix is the best mitigation under v0.1.6's spec constraints (the v0.1.7 candidate cluster below provides structural-resolution paths). **AC3 500-word cap accepted violation: 542/500 (Path B at Gate 4)** — `.claude/hooks/verify-all.sh:28` enforces the cap but Path B was the cleanest tradeoff against AC5's verbatim template + AC1's (a)–(f) coverage + AC2 strict-additivity. Merged 2026-05-18 via PR #42 (`ecae83f`, 6 commits — `c760a31` feat + plan artifact, `8694830` 2 new pitfalls, `ac5ddf0` AC5 outer-quote fix (cubic P2), `ecf7147` inline gate-override (cubic P1 best-effort), `22e180f` 5 v0.1.7 candidates in epic, `188236d` plan file post-implementation addenda). T2 synthetic test showed agent did NOT emit verbatim AC5 template at runtime — returned free-form prose with correct information but wrong format; Risk 5 closure deferred to v0.1.7 dogfood (the new v0.1.7 candidate cluster — AC2/AC4-AC1 contradiction + AC5 anchoring + cap hardening + empty-error fallback + all-fail branch — provides the structural-resolution path).

- **Two pitfalls captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E04.S8** (commit `8694830`): **(L54) LLM agents under-honor "Emit this exact summary: `<template>`" instructions.** Agent prompts with "Emit this exact summary: `<verbatim template>`" can produce free-form prose containing the right information but ignoring the specified format. LLMs interpret "exact summary" as a soft content hint, not a hard format constraint. Fix: imperative MUST language ("Your return summary MUST literally begin with…"), code-fenced template on own line with strong preamble, or post-emit self-check. Especially critical for agents whose returns are programmatically parsed downstream. **(L56) A clause inserted as a peer sub-bullet inherits the outer step's gate.** A new sub-bullet under an existing gated Process step is silently inactive for every dispatch shape that does not satisfy the parent gate. Agent process steps are gated as a whole unit; sub-bullets syntactically inherit the parent's gate even when the clause's intended precondition is different or unconditional. Fix: place unconditional behavior outside any gated step (as a standalone process step or in an explicitly ungated section); if structurally forced to remain nested, the clause must explicitly override the inherited gate. Review every new sub-bullet's effective gating condition before merge.

- **Eight v0.1.7 candidates surfaced from E04.S8** (commit `22e180f`, recorded in [E04 epic v0.1.7 candidates](docs/planning/epics/E04-path-consolidation-and-process-codification.md#v017-candidates)): five form a coherent doc-writer-failure-handling cluster (AC2/AC4-AC1 spec contradiction; AC3 500-word cap hardening; AC5 strict-format LLM-anchoring; empty `Edit` error-output fallback; all-fail branch); three are process observations (epic-reviewer/plan-reviewer AC-mutual-satisfiability check; plan-implementation drift handling at Stage 8 wrap-up; cubic-readable known-issues mechanism for accepted-violations). Recommended landing: pull the failure-handling cluster forward in v0.1.7 PM as a coherent unit; the process-observation trio can land separately or alongside.

- **E04.S7 — ADR-011: Skill flags as public API, env vars as debug-only.** Codifies the precedent surfaced by S11b-2 OQ1 (2026-05-08): user-facing skill behavior changes are expressed as flags in `$ARGUMENTS`, not environment variables. New [docs/adrs/ADR-011-skill-flags-as-public-api.md](docs/adrs/ADR-011-skill-flags-as-public-api.md) (49 lines) follows ADR-008/ADR-009 format precedent with `**Status:** Accepted`. Decision section provides an explicit threshold test for "user-facing": observable in pipeline outputs, gate decisions, or dispatched-model selection — surfaces the operator can detect without inspecting shell state or CI configuration. Consequences/Negative section covers the env-var carve-out for debug-only contributor-facing settings (positive example `ROUGHLY_HAIKU_BUDGET_USD`; negative example `ROUGHLY_SKIP_REVIEW=1`). Forward Reference names v0.2.0's complexity flag as the first downstream consumer by role only (no syntax detail), honoring the project's "frame forward refs by role" convention. [CLAUDE.md](CLAUDE.md) Key Design Decisions table gains an ADR-011 row; ADR enumeration at L17 updated to `(ADR-001 through ADR-009, ADR-011; ADR-010 reserved for v0.2.0 plan-format-v2)` to make the ADR-010 reservation visible (cycle 1 found the original "ADR-001 through ADR-011" form implied contiguity). [docs/adrs/README.md](docs/adrs/README.md) gains ADR-011 entry + ADR-010 reservation placeholder. [CONTRIBUTING.md](CONTRIBUTING.md) `## Skill authoring conventions` section (created in E04.S6) gains the one-line cross-reference: "User-facing skill behavior changes are flags, not environment variables (see ADR-011)." Merged 2026-05-15 via PR #41 (`b92e16f`, 3 commits — `92516b2` feat + `cef0c27` bold-grep pitfall capture + `0d2edb2` post-merge architect-review refinements). Pipeline: 1 plan-review cycle PASS first iteration; 2 Stage 6 review cycles (cycle 1 found 4 issues; cycle 2 clean); post-merge architect review (feature-dev:code-architect) returned 3 "Consider" + 1 actionable Nit — all applied (Decision threshold test; carve-out as rule not example-only; Forward Ref de-syntax; Negative consequences split into scannable bullets).

- **One pitfall captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E04.S7 T1** (commit `cef0c27`): **grep literal substrings break against bold-decorated markdown field labels.** `grep -Fn "Status: Accepted"` returns 0 hits when the file contains `**Status:** Accepted` — the literal byte sequence is `Status:** Accepted`, not `Status: Accepted`. Distinct from the existing metachar pitfall: the pattern is fine; the haystack decoration silently breaks the match. Pattern: when writing verify-step greps against markdown files with bold field labels, choose a search anchor that does not straddle a bold-close marker. Sibling pitfall to the regex-metachar pitfall already in the file. Known-pitfalls.md grew 80 → 82 lines as a result; back above the 80-line `>80` strict threshold; doc-writer's organize-suggestion will fire on the next pipeline write.

- **E04.S6 — Plan-discipline codification.** Three v0.1.5 retrospective deferrals bundled into one PR. Two new check bullets in [skills/review-plan/SKILL.md](skills/review-plan/SKILL.md) (96/300 lines post-merge): (1) "Every edit site enumerated" added to the Completeness dimension at L36–37 with a case-insensitive "structural uniformity" carve-out; canonical positive example `docs/plans/E03-S9-abort-prose-plan.md` (27-site enumeration); cites the `decision-table summary lines` pitfall (build L185 / fix L192; commit `015bb4d`). (2) "Runtime-signal source named" added to the Assumptions dimension at L44–45 with a policy-parameter carve-out distinguishing signal-source (correctness check) from threshold/comparator (maintenance check, explicitly out of scope of this AC); canonical positive example E03.S10's "if the failure output indicates a test failure" (`skills/build/SKILL.md` L180, `skills/fix/SKILL.md` L187); canonical negative example E03.S10 first-draft "if Stage 5c was hit by changes to test files" (commit `3c46687`). New `## Skill authoring conventions` section in [CONTRIBUTING.md](CONTRIBUTING.md) at L50–62 documenting (a) multi-branch case-dispatch language requirement with `skills/help/SKILL.md` Step 3 as canonical example, and (b) sequential-structure carve-out requiring BOTH ordered enumeration AND prose that positively asserts all-run-in-order semantics (opening invariant clause OR explicit previous/next-step references); functional role labels alone do NOT qualify. `skills/setup/SKILL.md` Step 5d Branch 4 cited as canonical sequential example. New fixtures directory at `tests/fixtures/review-plan/` with 7 synthetic fixtures (ac1-pass, ac1-needs-revision, ac1-carve-out-pass, ac2-pass, ac2-needs-revision, ac3-pass, ac3-needs-revision) + README documenting expected verdicts and the desk-check / subagent-dispatch verification paths. Merged 2026-05-15 via PR #40 (`9a18161`, 2 commits — `2823030` feat + `9d61030` post-pipeline correction of an invalid `claude /roughly:review-plan <path>` invocation example that was incompatible with the skill's `disable-model-invocation: true` frontmatter per ADR-001). Two cycles of post-implementation review: cycle 1 caught fabricated worked-example prose (the initial CONTRIBUTING.md sequential-structure carve-out cited setup/SKILL.md Branch 4 with quoted transition tokens that didn't exist in source — independently flagged by code-reviewer and silent-failure-hunter); cycle 2 caught over-permissive carve-out language ("functional role labels per step" as standalone qualifier let mutually-exclusive case structures claim the sequential carve-out — fix tightened to require an invariant clause OR explicit previous/next-step references). Two v0.1.7 candidates surfaced from this story; see [E04 epic v0.1.7 candidates](docs/planning/epics/E04-path-consolidation-and-process-codification.md#v017-candidates).

### Fixed

- **E04.S4 — Dogfood `.claude/hooks/verify-all.sh` cleanup.** Backports E03.S2's template-fix pattern to the dogfood Stop hook. Three coordinated changes at the top of [.claude/hooks/verify-all.sh](.claude/hooks/verify-all.sh) (was 58 lines pre-S4; 57 post-S4): (1) drop `set -e` from L6 — under `set -e`, `git rev-parse --show-toplevel` exiting non-zero outside a git repo killed the script before the `if [ -z "$ROOT" ]` no-op guard could run, breaking the exit-0 informational/non-blocking contract; (2) add `|| true` to the `git rev-parse` command substitution at L8 — defensive fail-soft even without `set -e`; (3) add `cd "$ROOT" 2>/dev/null || exit 0  # exit-0 contract: silent no-op on cd failure (see template L16–25)` guard at L12 — without `set -e`, an unguarded `cd` failure would silently continue script execution rather than emit an exit-0 no-op. The third change matches the template's coordinated changes that the original AC enumeration missed; classified as backport-completeness rather than scope creep (the template's own comment block names `cd` failure as the exact class `set -e` removal makes you responsible for guarding). Merged 2026-05-14 via PR #39 (`3017861`, 5 commits — `9e665f2` initial fix + `614fe6a` cd guard backport + 3 follow-ups including `3e0a88d` final annotation). Hook is still informational-only / exits 0 unconditionally / silent no-op outside the plugin repo.

### Changed

- **`.roughly/known-pitfalls.md` reorganized** during E04.S4 wrap-up. Doc-writer's post-write organize-suggestion fired (file at 82 lines, ≥80 threshold per E03.S3's retired-but-folded-in `pitfalls-organized-v1` trigger). 29 entries reorganized into 6 sections preserving content verbatim: Domain-Specific, Data & State, Build & Deploy, Skill & Agent Authoring (new), Documentation Hygiene (new), Planning & Scoping. Empty "Integration" and "Testing" stub sections removed. Final line count: 80 lines.

- **One pitfall captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E04.S4 wrap-up** (commit `614fe6a` discovery, recorded post-merge): **backport-from-template stories hide completeness gaps when the plan enumerates per-edit changes instead of diffing the final result against the reference.** The S4 plan enumerated 2 edits; the template at `skills/setup/templates/verify-all-stop-hook.sh.template` had 3 coordinated changes; the third (`cd "$ROOT" 2>/dev/null || exit 0`) was missed at plan-write, plan-review (cycle 2 PASS), Stage 6 (all 3 reviewers — silent-failure-hunter flagged but classified negligible), and Stage 7 verify-all — surfaced only by post-commit `cubic review`. Two commits were needed: `9e665f2` (the enumerated two edits) and `614fe6a` (the missed third edit). **Pattern:** any backport-from-reference story must add an explicit Stage 7 verification step that diffs the result against the reference at the "does the result match the reference" level — per-edit checks are insufficient. Directly applicable to E04.S5 (Stop hook drift coverage expansion — another backport-shaped story).

- **E04.S1 — Plan-path consolidation `docs/plans/` → `.roughly/plans/`.** All 29 historical plans relocated via `git mv` (E02.S2.6 history-preserving precedent). 17 path-string substitutions across 5 skill bodies (build L85/L122, fix L96/L129, help 10 refs, audit-epic L44, review-plan L36/L44 — the 2 review-plan refs were unenumerated in the original story spec but required by AC1's verify-command zero-match invariant; discovered and corrected during Stage 2 of the build pipeline). Pre-flight migration check extended from one-form (`.ruckus/`) to two-form (`.ruckus/` OR `docs/plans/`) across 7 hard-abort skills + setup soft-abort, with new `<!-- pre-flight:start -->` / `<!-- pre-flight:end -->` HTML comment delimiters on the 7 hard-abort skills (setup explicitly excluded from byte-identity check per `.roughly/known-pitfalls.md` L40 E03.S4 known-pitfall). New [tests/fixtures/canonical-preflight-block.txt](tests/fixtures/canonical-preflight-block.txt) as single source of truth for the two-form block — consumed by E04.S5's planned drift check. New v0.1.6 migration step in [skills/upgrade/SKILL.md](skills/upgrade/SKILL.md) (3-point: detect + safety + move + cleanup; `--force-plans` opt-in for dirty `docs/plans/`; `git mv` inside git, plain `mv` otherwise, no fallback per design decision). [scripts/ci-dogfood.sh](scripts/ci-dogfood.sh) L153/L156 assertion paths updated. README.md L214 prose updated. CONTRIBUTING.md gains `## Migration` section documenting the path move. No new ADR — matches v0.1.2 `docs/claude/` → `.ruckus/` and v0.1.4 `.ruckus/` → `.roughly/` precedents. **Spec finding:** AC1's literal verify `rg -Fn "docs/plans" skills/` cannot return 0 matches because the new pre-flight blocks intentionally contain `docs/plans/` as legacy-state detection text, the setup soft-abort prose also names it, and the new v0.1.6 migration step in `skills/upgrade/SKILL.md` references it as the legacy path it moves. Intent-correct verify: `rg -Fn "docs/plans" skills/ | grep -v "pre-flight" | grep -v "setup/SKILL.md" | grep -v "upgrade/SKILL.md"` returns 0 matches, i.e., no runtime/active path references outside the documented exclusion set (pre-flight blocks, setup soft-abort, upgrade migration step). **Post-commit cubic refinement (6 iterations to clean):** (a) Marker file relocated from destination `.roughly/plans/.migration-in-progress` to source `docs/plans/.migration-in-progress` — writing to the destination would create `.roughly/plans/` and break the directory rename; the v0.1.4 marker-at-source idiom is the correct pattern (the rename carries the marker along atomically). (b) Step 1 destination-conflict abort added: if `.roughly/plans/` already exists with content (other than a `.migration-in-progress` marker), abort with manual-resolution guidance. (c) Step 1 now `rm -rf` empty/marker-only `.roughly/plans/` before move — `git mv` into an existing destination directory NESTS the source under it (producing `.roughly/plans/plans/`) instead of renaming. (d) Pre-flight signal evolved through three designs: first attempt used bare `docs/plans/` directory presence (cubic flagged: false-positive for any project with unrelated `docs/plans/` documentation), second attempt added `.roughly/` + `.roughly/plans/`-absence as gates (cubic flagged: setup unconditionally creating `.roughly/plans/` defeated the signal), final design uses `*-plan.md` filename pattern as the Roughly-plans signal: `.roughly/` exists AND any file matching `docs/plans/*-plan.md` exists. (e) Setup reverted to NOT creating `.roughly/plans/` — the directory is created on-demand by build/fix Stage 3's `mkdir -p` instruction; with the `*-plan.md` signal, pre-pre-creating `.roughly/plans/` is no longer needed. Canonical block hash evolved: `cc6eb904...` (initial) → `aba9d38a...` (`.roughly/` co-existence) → `da9e25e1...` (`.roughly/plans/` absence) → `8c03ed35...` (final `*-plan.md` design). **Known limitation (accepted):** a project that has BOTH `.roughly/` (Roughly install) AND `docs/plans/*-plan.md` files using the same naming convention but for unrelated planning documentation will hit a false-positive abort. The naming-convention collision is rare (the `<name>-plan.md` pattern is specifically Roughly's convention) and the workaround is straightforward (rename collision files OR move them out of `docs/plans/`). Accepted given v0.1.x's single-known-user adoption stage; the current Roughly maintainer's own projects intentionally follow the `docs/plans/*-plan.md` convention, so this case is expected to fire correctly for them.

### Migration

v0.1.5 → v0.1.6 introduces one user-action step: run `/roughly:upgrade` from each project that has historical plans in `docs/plans/`. The upgrade detects the legacy directory, performs `git mv docs/plans/ .roughly/plans/` inside a git repo (preserves history per E02.S2.6) or plain `mv` otherwise, and updates no other files. If `docs/plans/` has uncommitted edits, the migration aborts; pass `--force-plans` to override (e.g., when you are intentionally migrating a dirty tree). Idempotent: re-running after a successful migration is a no-op.

The pre-flight migration check in `/roughly:build`, `/roughly:fix`, `/roughly:audit-epic`, `/roughly:review`, `/roughly:review-plan`, `/roughly:review-epic`, `/roughly:verify-all`, and `/roughly:setup` now detects either `.ruckus/` (v0.1.3 install) or `docs/plans/` (pre-v0.1.6 location) and redirects to `/roughly:upgrade`. Skills will abort with the two-form prose until both legacy directories are absent (or, for setup, you choose `proceed anyway`).

## [0.1.5] — 2026-05-13

> Trust hardening + ergonomics + CI. Scope per [docs/planning/epics/E03-trust-and-ergonomics.md](docs/planning/epics/E03-trust-and-ergonomics.md).

### Added

- **E03.S11a — Plugin self-test CI scaffolding.** Establishes the isolation contract for plugin dogfood CI. New [.github/workflows/dogfood.yml](.github/workflows/dogfood.yml) runs on push to `main` and on pull_request; the `dogfood-build-cycle` job invokes [scripts/ci-dogfood.sh](scripts/ci-dogfood.sh) (new), which sets up an ephemeral worktree at `/tmp/roughly-dogfood-${SHA}`, registers `trap cleanup EXIT` before worktree creation, includes a stale-worktree guard for same-SHA reruns, and verifies no source-tree pollution via `git status --porcelain` symmetry pre/post run. The `claude` invocation point is a **no-op stub** — real CLI invocation lands in S11b-1 (smoke test) and S11b-2 (full scenario), per AC. `ANTHROPIC_API_KEY` is scoped at step-level only (sourced from `secrets.ANTHROPIC_API_KEY`) — least-privilege exposure consistent with the stub not consuming it; S11b-1 will add its own step-level `env:` block when it actually consumes the secret. [CONTRIBUTING.md](CONTRIBUTING.md) gains a `## CI` section between Testing and License covering: workflow logs location, local repro steps, in-scope vs out-of-scope for v0.1.5 CI, token-cost expectations (S11b-1 ~5K, S11b-2 ≤150K Sonnet tokens), and auth secret setup. CI cluster now 2/3 — S11b-2 happy-path remains.

- **E03.S11b-1 — CLI plumbing smoke test.** Replaces the no-op stub in [scripts/ci-dogfood.sh](scripts/ci-dogfood.sh) with a real `claude --bare --plugin-dir "$WORKTREE" --no-session-persistence -p` invocation that proves auth and plugin-load on every CI run. Two acceptance anchors drive the smoke: slash-command listing (`/roughly:setup` must appear in output) and an `Invalid API key` recognizable error string for the auth-failure negative-test path. The auth-failure negative-test step in [.github/workflows/dogfood.yml](.github/workflows/dogfood.yml) runs unconditionally on every push/PR as an ongoing regression guard — a deliberately invalid `ANTHROPIC_API_KEY=invalid-key-xyz` is scoped to that step alone, keeping the real secret confined to the `Run dogfood scaffolding` step's `env:` block and never hoisted to workflow-global scope. A new `Install Claude Code` step (`npm install -g @anthropic-ai/claude-code@2`) provides the binary on `ubuntu-latest` ahead of both invocations — pinned to major v2 to block unannounced breaking releases from silently flipping CI green-to-red while still tracking stable point/minor updates within the major. `--max-budget-usd 0.05` enforces AC5's ≤5K-token cost cap as a hard gate. [CONTRIBUTING.md](CONTRIBUTING.md) gains auth-requirement prose in the `## CI` section reflecting that the smoke step now actually consumes `ANTHROPIC_API_KEY`. CI cluster now 2/3 — S11b-2 happy-path remains.

- **E03.S11b-2 — Scripted dogfood happy-path build cycle.** Drives a full `/roughly:build` cycle end-to-end in CI against a minimal fixture, without a human at Stage 4's plan-review gate. New `--ci` flag added to [skills/build/SKILL.md](skills/build/SKILL.md) (296 → 298 lines, +2 net after a compression-for-headroom round in review-fix 1 merged Stage 1 detection into the existing parse paragraph): Stage 1 detects `--ci` in `$ARGUMENTS` and sets internal `CI_MODE=true`; Stage 4 skips the blocking `/roughly:review-plan` dispatch and emits the literal marker `[--ci] plan review skipped — synthetic PASS` (em-dash U+2014, byte-identical contract with the CI grep). All other stages run normally — the only mechanical bypass is Stage 4's blocking subagent dispatch; conversational gates self-progress under `claude -p`. The puncture is documented inline as a deliberate ADR-001 carve-out for non-interactive CI execution; explicit "never invoke `--ci` interactively" prose discourages future contributors from treating this as precedent for skipping review-plan elsewhere. New minimal fixture at [tests/fixtures/hello-roughly/](tests/fixtures/hello-roughly/) (CLAUDE.md + `src/greeter.sh` + `tests/greeter.test.sh`) — a target Bash project (not a Roughly install), single-task buildable feature ("add a NAME constant"), chosen for minimum CI token cost. [scripts/ci-dogfood.sh](scripts/ci-dogfood.sh) extended with a full-scenario block (between the smoke + plugin-load success echo and the existing post-state pollution check) that `cd`s into the worktree's copy of the fixture and invokes `/roughly:build --ci` with `timeout 270` (4.5 min, under the 5-min AC ceiling) and `--max-budget-usd 1.50` (≈ 150K mixed Sonnet tokens at current pricing — enforces AC8 mechanically). Five structural assertions guard the happy path: synthetic-PASS marker present (AC5 regression guard against `--ci` drift), plan file exists in `docs/plans/`, plan contains `## Tasks` section, plan contains `### T1` task (per-S6-stable anchor across plan-format-version evolution), and `src/greeter.sh` shows the implementation actually ran. Each assertion failure dumps the offending state indented to stderr per the existing diagnostic style. Mandatory `&& SCENARIO_EXIT=0 || SCENARIO_EXIT=$?` exit-capture idiom per `.roughly/known-pitfalls.md` `set -e` + command-substitution pitfall. Fixture state reset between runs is satisfied by the S11a worktree-boundary contract — mutations land in `/tmp/roughly-dogfood-${SHA}/tests/fixtures/hello-roughly/` and are torn down by the `trap cleanup EXIT` handler; the existing post-state `git status --porcelain` symmetry check still runs as the final guard. [CONTRIBUTING.md](CONTRIBUTING.md) `## CI` section gains a `**--ci flag.**` paragraph documenting the mechanism, a fixture-path callout in the local-repro paragraph, and S11b-2 past-tensed to `(landed in this story)` matching the S11a/S11b-1 prose pattern. Token-cost target line preserved unchanged — the `--max-budget-usd 1.50` flag is the mechanical enforcement of the documented 150K-Sonnet-token ceiling. CI cluster now 3/3 complete — v0.1.5 CI scope met.

- **E03.S12.0 — Resolve roughly.dev source location (decision-only, no PR).** Three options were considered: (a) in-repo `docs/site/*.md`, (b) separate `roughly-dev` repo, (c) defer the docs cluster from v0.1.5. **Decision: (c) defer.** Rationale: v0.1.5's release thesis is "enforcement with known holes is theater"; docs document what got filled but aren't themselves a hole. [docs/planning/README.md:84](docs/planning/README.md#L84) (post-ROADMAP authoring) calls roughly.dev "out of repo scope; tracked separately" and represents the more-recent intent. Path-to-v1.0 criterion #5 ("roughly.dev complete enough that a stranger gets the pipeline without reading SKILL.md") is explicitly a v1.0 milestone, not v0.1.5. Implementation-story scope reduced from 15 → 13 (S12a + S12b removed from epic and tracked in v0.1.6 candidates for separate-repo handling; S12.0 retained as a historical decision record outside the implementation-story count). [docs/ROADMAP.md](docs/ROADMAP.md) item #12 marked deferred. Resolved 2026-05-08.

- **E03.S6 — Plan-format version field.** Forward-compat marker line `Plan-format-version: 1` added to the Stage 3 plan template in [skills/build/SKILL.md](skills/build/SKILL.md) and [skills/fix/SKILL.md](skills/fix/SKILL.md), placed between the plan title and the next section (`## File Table` in build; `## Root Cause` in fix). Format mirrors `.roughly/workflow-upgrades` style: single-line key-value, no frontmatter delimiters, no HTML comment — greppable with `rg '^Plan-format-version:'`. **Nothing reads the field in v0.1.5.** It exists so v0.2.0's plan-format-v2 migration step (per ADR-010, formerly ADR-009 before S1's plan-mode-detection ADR took that slot) can detect existing v1-format plans. [skills/review-plan/SKILL.md](skills/review-plan/SKILL.md) is unchanged — it does not validate, parse, or branch on the version field. No new ADR (rationale folds into v0.2.0's plan-format-v2 ADR). Trust-hardening cluster now 7/7 complete.

- **E03.S4 — Pre-flight migration check in remaining 2 skills.** Extended the canonical `.ruckus/` legacy-state abort block from 6 skills to 8: [skills/audit-epic/SKILL.md](skills/audit-epic/SKILL.md) (139 → 141 lines) and [skills/verify-all/SKILL.md](skills/verify-all/SKILL.md) (78 → 80 lines) now contain the byte-identical hard-abort preamble matching build/SKILL.md L19. `/roughly:upgrade` remains excluded by design (it IS the migration target). [docs/ROADMAP.md](docs/ROADMAP.md) item 4 marked complete with `✅ Done — landed in E03.S4.` marker. **Notable design decision:** [skills/setup/SKILL.md](skills/setup/SKILL.md) is intentionally NOT normalized to the canonical hard-abort form — its two-line prose+blockquote with `(proceed anyway / abort)` soft-override is deliberate, since setup is the install skill and hard-aborting would prevent legitimate install-time recovery from legacy state. AC3's "identical surrounding context" requirement is therefore scoped to the 7 hard-abort skills (audit-epic, build, fix, review, review-plan, review-epic, verify-all); setup's form is the intentional 8th, not drift. Decision persisted in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) Build & Deploy section. Trust-hardening cluster now 6/7 (S6 plan-format version field remains). Merged 2026-05-07 via PR #29 (`b3a6750`, 2 commits — `1062432` core extension + `2b660e2` setup soft-abort pitfall).
- **One pitfall captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E03.S4 wrap-up (commit `2b660e2`):** setup soft-abort pre-flight is intentional, not drift — applies when reviewing the 8-skill pre-flight uniformity invariant. Future contributors should not "fix" setup's form to match the 7 hard-abort skills.

- **E03.S5 — Document `Edit` `replace_all` dual-semantic-token failure.** New `## Tooling Pitfalls` section in [CONTRIBUTING.md](CONTRIBUTING.md) (between `## Code Standards` and `## Testing`) documents the bulk-replace dual-semantic-token failure mode discovered in the S02.7 `verify-all.sh` rename incident. Names the failure mode (`Edit replace_all: true`), the at-risk tools (`Edit`, `sed -i`, IDE find/replace), and includes a worked example with verification commands (`rg -nw 'old-token' <file>` and `rg -nw 'new-token' <file>` with claimed counts/lines: 3 @ L17/18/19 and 2 @ L2/11). Self-verification holds against the live [.claude/hooks/verify-all.sh](.claude/hooks/verify-all.sh) today. Section landed at 20 content lines (15-30 budget). Prose-only — no skill/agent/hook changes. Merged 2026-05-07 via PR #28 (`b58f4bd`, 2 commits — `ef3a350` core section + `b8abb69` cross-file line-number-rot pitfall).
- **One pitfall captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E03.S5 wrap-up (commit `b8abb69`):** cross-file line-number rot — line-number citations in CONTRIBUTING.md L66–67 can rot silently if the referenced file (`verify-all.sh`) is edited later. Recorded informally rather than promoted to ADR.

- **E03.S2 — Stop-hook-v1 maturity check completion.** Closes the no-op gap in the `stop-hook-v1` maturity check: pre-S2, accepting the offer at build/fix Stage 8 recorded a `workflow-upgrades` entry but installed nothing. New project-agnostic template at [skills/setup/templates/verify-all-stop-hook.sh.template](skills/setup/templates/verify-all-stop-hook.sh.template) (~95 lines, fast checks only — type-check default, lint/format opt-in, test/build excluded; bash-3+ compatible; jq → python3 → hand-built JSON 3-tier `emit_drift_json` runtime fallback; always exits 0 for the informational/non-blocking contract). The canonical install path lives in [skills/setup/SKILL.md](skills/setup/SKILL.md) Step 5d Branch 4 — a 4-phase transactional commit (Step A read-only validation; Step B settings-conflict decision with `keep`/`replace`/`merge`/`decline`; Step C file-conflict decision; Step D commit phase using stage-then-promote with snapshot rollback and cascade-failure handling). Step 6 adds the offer to initial setup for Established projects with a configured type-check command. [skills/build/SKILL.md](skills/build/SKILL.md) and [skills/fix/SKILL.md](skills/fix/SKILL.md) Stage 8 `stop-hook-v1` blocks reframed as a lighter install path that delegates conflict UX to setup. Recording semantics split: `-added` for successful install/replace/merge, `-declined` for active rejection, no record for passive preservation (re-offered next run). Build/fix Stage 8 blocks remain byte-identical (manual sync per ADR-003 reference-copy pattern). Merged 2026-05-06 via PR #27 (`a3d7afc`, 24 commits — 1 feat + 1 docs + 22 fix/refinement).
- **Deferred-investigations catalog.** New [docs/deferred-investigations.md](docs/deferred-investigations.md) tracks process and quality observations surfaced during execution but out of scope for the work that surfaced them. Distinct from ROADMAP (committed work) — investigations are noticed-but-not-yet-evaluated; pull from the catalog when scoping each release. Seeded with **DI-001: Stage 6 review depth vs external review tools** — observed in S2 wrap-up where Stage 6's parallel reviewers closed 7 findings pre-commit while ~17 cubic rounds and a proactive 4-agent parallel review surfaced ~25 additional valid findings post-commit. Five hypotheses and four investigation directions captured. Candidate placement: v0.1.6 trust-hardening cluster.
- **ROADMAP.md "Deferred investigations" section** linking to the new catalog.
- **Two pitfalls captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E03.S2 wrap-up:** conflict-prompt cleanup completeness; tool-availability validation before file writes.

- **E03.S3 — Retire `test-verify-v1` and `pitfalls-organized-v1` maturity checks.** Stage 8 maturity-check loop reduced from 5 → 3 active checks (CLAUDE.md quality, `investigator-v1`, `stop-hook-v1`). The retired checks' work now lives in [agents/doc-writer.md](agents/doc-writer.md) Process step 5, which fires post-write conditionally on `wc -l > 80` for organize-suggestions and on test-config detection (`package.json`/`pytest`/`vitest`/`jest`/`pyproject.toml`) for test-integration suggestions. Coverage gap is two-part-gate: suggestions fire only when (a) the user confirms new pitfalls/conventions at wrap-up AND (b) doc-writer actually writes to `.roughly/known-pitfalls.md`. Manual edits are explicitly out of the new trigger surface; documented in agents/doc-writer.md and [README.md:221](README.md#L221). Existing `*-declined` entries in users' `.roughly/workflow-upgrades` are not auto-cleaned (verified inert). Build/fix line counts: 297 → 288 and 300 → 291 (12 and 9 lines headroom for downstream stories). Merged 2026-05-04 on `feat/S03.3-retire-test-verify-v1` (8 commits — `33bf966` core, plus 7 follow-ups including `9078a5f` fix/SKILL.md Stage 8 wrap-up harmonization to ask about conventions, matching build).
- **ADR-005 fourth disposition: retirement.** [docs/adrs/ADR-005-versioned-maturity-checks.md](docs/adrs/ADR-005-versioned-maturity-checks.md) gains a v0.1.5 retirement footnote and a Consequences/Negative bullet acknowledging retirement as a formal disposition alongside add/decline/version-bump. Future stories that retire checks should follow the same pattern (footnote + Consequences/Negative bullet).
- **Two pitfalls captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E03.S3 wrap-up (commit `9440ecb`):**
  - LLM agent conditionals need explicit failure-handling clauses — discovered when first-pass review of doc-writer step 5 produced 3 P1 silent-failure findings on what looked like a simple two-bullet conditional addition.
  - Pre-existing markdown lint warnings on `PostToolUse:Edit` hooks are unrelated to changed lines — discovered during T5 ROADMAP edit (~30 warnings on untouched lines).

- **E03.S1 — Plan-mode auto-detect/exit at Stage 1 of build/fix (highest-value v0.1.5 item).** When `/roughly:build` or `/roughly:fix` is invoked while Claude Code's plan mode is active, the new `UserPromptSubmit` hook at [.claude/hooks/plan-mode-gate.sh](.claude/hooks/plan-mode-gate.sh) reads `permission_mode` from stdin and emits `decision: "block"` JSON, preventing the prompt from reaching the model. Per-skill scoping (restricting enforcement to `/roughly:build` and `/roughly:fix`) happens inside the hook script via `prompt`-field regex, since Claude Code's hook `matcher` field is tool-name only. Hook posture is fail-closed (no `set -e`, parser-tool guards, explicit fail-closed branches at every parsing failure point). Templated into user projects via [skills/setup/templates/plan-mode-gate.sh.template](skills/setup/templates/plan-mode-gate.sh.template) and a new `UserPromptSubmit` registration in [skills/setup/templates/settings.json.template](skills/setup/templates/settings.json.template), deployed by `/roughly:setup` Step 5d (three-branch registration with `jq`/JSON preflights and warning escalation). Build/fix preambles updated at L11 (substitution-only; line counts preserved at 296/299). Closes ADR-001's enforcement gap for plan-mode hijack. Authoritative artifact: [docs/adrs/ADR-009-plan-mode-detection.md](docs/adrs/ADR-009-plan-mode-detection.md). Merged 2026-05-02 via PR #25 (`c598ef6`); follow-up fixes `5b09864`, `a299ada`, `6d268e9`, `2c1436b` through 2026-05-03.
- **ADR-009 — Plan-Mode Detection.** New ADR documenting: detection mechanism, Step 1 empirical verification result (PASS, 2026-05-02), spike-doc API-name correction (`UserPromptExpansion` → `UserPromptSubmit`), distinction from ADR-001 (invocation gate vs mid-pipeline stage gate; harness-owned data vs pipeline-internal data), hook registration scope (in-script `prompt`-field matching since `matcher` is tool-name only), hook posture (fail-closed), open items. ADR-001 through ADR-009 now cited in [CLAUDE.md](CLAUDE.md); [docs/adrs/README.md](docs/adrs/README.md) gains a "Current ADRs" list. The plan-format-v2 ADR previously slotted as ADR-009 in v0.2.0 is bumped to ADR-010 (ADRs are numbered by landing order).
- **`UserPromptSubmit` hook templating in `/roughly:setup`.** Step 5d gains a three-branch registration flow (no settings.json → write fresh; existing settings.json with no `UserPromptSubmit` → merge; existing with `UserPromptSubmit` → preserve and warn) plus `jq`-availability and JSON-validity preflights surfacing blocking warnings in Step 7.
- **E03.S0 — Plan-mode detection spike (½-day).** [docs/planning/spikes/plan-mode-detection-findings.md](docs/planning/spikes/plan-mode-detection-findings.md) maps the design space for plan-mode auto-detection at Stage 1 of build/fix. **Conclusion:** preamble + lightweight hook — `UserPromptSubmit` (corrected from spike's `UserPromptExpansion` during S1; see [ADR-009 §Spike-Doc Correction](docs/adrs/ADR-009-plan-mode-detection.md)) reading `permission_mode` from hook stdin (documented value `"plan"` when plan mode is active). Implementation gated to E03.S1's empirical-verification step. Spike findings doc preserved per AC10 retention decision. Merged 2026-05-01 via PR #24 (`e5d630f`).
- **Five pitfalls captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E03.S0/S1 wrap-up:**
  - *Planning & Scoping* — `permission_mode` in hook stdin is a programmatic plan-mode signal that planners routinely miss (S0, commit `a4a4726`).
  - *Planning & Scoping* — "Confirmed empirically" ACs in spike stories can be unachievable from inside an active pipeline session (S0, commit `a4a4726`).
  - *Planning & Scoping* — Spike-doc external API identifiers must be re-verified against the live API at implementation start (S1, commit `2c1436b`; surfaced when the spike's `UserPromptExpansion` propagated unchecked through plan + plan-review PASS).
  - *Build & Deploy* — Markdown-list-nested heredocs in copy-paste-ready docs are indentation-fragile and silently break (S1, commit `2c1436b`; recommends `printf '%s\n' ...` over `cat <<EOF` inside numbered lists).
  - *Build & Deploy* — `set -euo pipefail` combined with unguarded parser tools silently opens safety-gate hooks (S1, commit `2c1436b`; drop `set -e`, keep `set -uo pipefail`, audit every parser-tool call for fail-closed alternatives).

- **E03 audit and doc-hygiene remediation (2026-05-13, PR #37).** Post-implementation audit of all 96 ACs across the 13 E03 implementation stories + S12.0 decision. Verdict: **91 MET / 5 PARTIAL / 0 NOT MET**; cross-cutting consistency PASS; zero regressions; shipping-ready. Audit report at [docs/planning/epics/E03-trust-and-ergonomics-audit.md](docs/planning/epics/E03-trust-and-ergonomics-audit.md). Three doc-hygiene fixes applied inline to [docs/planning/epics/E03-trust-and-ergonomics.md](docs/planning/epics/E03-trust-and-ergonomics.md): S1 AC6 line-count annotation, S3 AC6 line-count annotation, S2 AC5 gate-asymmetry rationale documented at the AC site (previously only in epic prose). The 5 PARTIALs: 1 S0 empirical-limitation gap (AC2 — auto-engagement triggers documented from official Claude Code docs but not exercised empirically because the spike ran inside an active pipeline session; forwarded to S1 and resolved there), 2 stale line-count AC text claims (S1 AC6 + S3 AC6 — cumulative additions from later stories drifted the spec away from end-state), 1 intentional setup-vs-pipeline gate asymmetry undocumented at the AC site (S2 AC5), and 1 over-delivery (S11b-2 ships 6 structural assertions vs spec'd 5).

- **One pitfall captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E03 audit-fix Stage 4 review-plan (commit `fdd5aac`):** plan `Verify:` regex-metachar hazard — when a plan's `Verify:` literal-substring grep uses `grep -n "..."` and the pattern contains regex metacharacters (`.`, `*`, `?`, `[`, etc.), the grep can silently return zero matches even after a correct edit because the metachar is interpreted as regex. Default to `grep -Fn '...'` (fixed-string flag with single quotes) for any plan `Verify:` literal-substring check. Pitfalls now at 82/80 — over the organize threshold; doc-writer's S3 organize-suggestion will fire on the next pipeline write.

- **E03.S8 — `/roughly:help` command.** New 10th skill at [skills/help/SKILL.md](skills/help/SKILL.md) (163 lines, 54% of 300-line cap; `disable-model-invocation: false` — interactive informational skill, never aborts). Output structure: (1) commands grouped by cluster (pipeline / coordinator / utility); (2) current `.roughly/workflow-upgrades` maturity-check state (which are added, declined, available); (3) current pipeline state via branch-association heuristic over `docs/plans/*-plan.md`. **Step 3 (in-progress plan detection)** evolved through 9 cubic-driven fix iterations into a mutually-exclusive case dispatch — Case A (no plans, terse); Case B (main-line branch, terse); Case C (unknown/detached-HEAD branch OR feature-branch zero-match, surface up to 10 candidates with `ls -lt docs/plans/` escape if N>10); Case D (feature-branch single-match, identified as in-progress); Case E (feature-branch multi-match, disambiguation prompt). Branch normalization strips `feat/|fix/|chore/|docs/|bug/|wip/` prefixes, replaces `.` with `-`, lowercases; substring match in either direction against plan filename minus `-plan.md`. User-response handling: whitespace-trimmed, case-insensitive `none`, exact filename match, max-3-retry graceful exit so help never blocks the session. Pre-flight migration check is note-only (never aborts) — help is the recovery path itself, analogous to `/roughly:upgrade`, and the 8-skill pre-flight invariant from S4 explicitly excludes help by design. [README.md](README.md) gains rows in "Choose Your Workflow" + "Skills Reference"; [CLAUDE.md](CLAUDE.md) structure-table skill count bumped 9 → 10. Plugin loads with 10 skills (auto-discovery; no manifest edit required). Merged 2026-05-12 via PR #36 (`740ab5f`, 10 commits — 1 feat + 1 docs + 8 fix iterations on Step 3 + post-merge cubic refinements).

- **Two pitfalls captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E03.S8 wrap-up (commit `10b5d1c`):** (1) Glob has no mtime — when a skill needs filesystem mtime (sort, display, age comparison), use `ls -lt`/`stat`/`find -printf`, **not** Glob (companion to the existing "observable signals" conditional-grounding pitfall from S10). (2) Utility-skill retry loops need max-retry caps — pipeline skills (`disable-model-invocation: true`) inherit the cap discipline from build/fix Stage 5c, but utility skills (`disable-model-invocation: false`) inherit the prompt pattern without the caps. Pattern: "after N invalid responses, emit recovery message and end the step without picking."

- **Two pitfalls captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E03.S9 wrap-up (commit `015bb4d`):** (1) Decision-table summary lines are discrete emit sites, not restatements — when sweeping a "If X: A. If Y: B. If Z: C." block, trailing summary lines must be enumerated as separately numbered edit sites in the plan, not left as "confirm during edit" footnotes. Surfaced when the plan's "confirm during edit" footnote masked real edit sites at L185 (build) / L192 (fix) — caught independently by both code-reviewer and silent-failure-hunter in cycle 1. (2) Inline backtick-quoted emit strings followed by additional prose take exactly one period — inside the backtick — placing periods both inside and outside the closing backtick produces an ambiguous boundary for LLM parsing.

- **One pitfall captured in [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) during E03.S10 wrap-up (commit `3c46687`):** LLM-interpreted conditionals must reference observable signals, not abstract categories. Complementary to the existing "explicit failure-handling clauses" pitfall — grounding + failsafe pair now both documented. Surfaced when S10's auto-fix-cap test-fix conditional was initially framed as "if Stage 5c was hit by changes to test files" (no test-file detection mechanism existed); reframed to "if the failure output indicates a test failure — assertion errors or test-runner output" (observable signals, project-agnostic).

### Changed

- **Plan-mode hijack pitfall recategorized.** [.roughly/known-pitfalls.md](.roughly/known-pitfalls.md) Domain-Specific entry rewritten from "open silent failure" to "blocked by S1 enforcement (ADR-009)" with recovery instructions and a caveat for users whose `.claude/settings.json` already has a `UserPromptSubmit` entry (setup will not overwrite — verify manually that plan-mode protection is in place).
- **Abort prose hardened across pipeline skills (E03.S9).** Every abort / escalation branch in [skills/build/SKILL.md](skills/build/SKILL.md), [skills/fix/SKILL.md](skills/fix/SKILL.md), and [skills/review-plan/SKILL.md](skills/review-plan/SKILL.md) now emits a 4-field message: **stage**, **reason**, **state of files** (plan exists/doesn't, implementation files staged/unstaged), **recovery action**. 27 substitution-only edits (13 in build + 13 in fix + 1 in review-plan); net-zero line delta across all three files. Positive regex `Stage .* (aborted|stopped|cannot proceed)` matches at every site; `Recovery: <action>` greppable marker present. Negative regex `aborted\b` paired with `Stage` (zero unpaired hits). [skills/review-epic/SKILL.md](skills/review-epic/SKILL.md) and [skills/audit-epic/SKILL.md](skills/audit-epic/SKILL.md) verified out of scope — no in-pipeline abort gates. ABORT HANDLING block at build L276-298 / fix L277-299 preserved byte-verbatim (MD5 verified). Per-site inline substitution chosen over a template block — template would have cost ≥2 lines in fix's 1-line headroom. Merged 2026-05-12 via PR #35 (`2a4dfdc`, 4 commits — `e0cecd7` core sweep + `015bb4d` pitfalls + `fe2a6ad` Stage 5c file-state field fix + `d9b624e` plan verify-command count correction).
- **Retry-loop caps tuned (OQ3 ratified 2026-05-10).** [skills/build/SKILL.md](skills/build/SKILL.md) and [skills/fix/SKILL.md](skills/fix/SKILL.md) Stage 5c/Stage 6 cap dispositions: questions cap **kept at 2** (OQ3 #1), auto-fix cap **raised from 2 to 4** for type-check and lint/format with **kept at 2** for test-command failures (OQ3 #2/#3/#4 via Path C — single unified cap with command-output-based test conditional), Stage 6 review-fix cycles **kept at 2** (OQ3 #5). Build/fix parity preserved. Inline OQ3 rationale annotations added per cap; no new ADR (rationale lives in the epic story body).

### Fixed

- **`PostToolUse` template schema.** [skills/setup/templates/settings.json.template](skills/setup/templates/settings.json.template) `PostToolUse` entry corrected to nested `{matcher, hooks: [{type, command}]}` shape (the prior flat shape was broken in Claude Code 2.x). Caught while adding the `UserPromptSubmit` registration in S1; without this fix, S1 would have shipped a working plan-mode hook alongside a broken formatter hook on user installs.

## [0.1.4] — 2026-04-30

> Hard-cut rename from `ruckus` to `roughly`. Behavior is identical to v0.1.3 — only names, paths, namespace identifiers, and the workflow-upgrades version-line identifier change.

### Changed

- Renamed plugin from `ruckus` to `roughly`. Hard cut with no aliases or backwards compatibility (E02.S2.1)
- Plugin name field updated in `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` from `ruckus` to `roughly` (E02.S2.1)
- Slash command namespace migrated from `/ruckus:*` to `/roughly:*` across all 9 skills — `/roughly:build`, `/roughly:fix`, `/roughly:review`, `/roughly:review-plan`, `/roughly:review-epic`, `/roughly:audit-epic`, `/roughly:verify-all`, `/roughly:setup`, `/roughly:upgrade` (E02.S2.1)
- Plugin-installed dotdir migrated from `.ruckus/` to `.roughly/` in skill bodies and v0.1.4 upgrade-migration logic (E02.S2.2)
- Workflow-upgrades version-line identifier renamed from `ruckus-version` to `roughly-version` across setup and upgrade skills (E02.S2.2)
- v0.1.2 upgrade migration step (`docs/claude/` → `.ruckus/`) modified to migrate directly to `.roughly/`, skipping the `.ruckus/` intermediate for users upgrading from v0.1.0/v0.1.1 directly to v0.1.4 (E02.S2.2)
- Agent preamble path reference updated from `.ruckus/known-pitfalls.md` to `.roughly/known-pitfalls.md` in `agents/agent-preamble.md` (canonical) and the 6 consumer agents (code-reviewer, discovery, epic-reviewer, investigator, silent-failure-hunter, doc-writer); `static-analysis.md` remains the documented exception (E02.S2.3)
- This repo's own dogfood `.ruckus/` directory renamed to `.roughly/` via `git mv`, with content updates in `known-pitfalls.md` (project name, domain description, command references, subagent_type example) and `workflow-upgrades` (version-line identifier). `git log --follow` continuity preserved across both v0.1.2 and v0.1.4 transitions (E02.S2.6)
- Setup templates updated for new project installs: `skills/setup/templates/CLAUDE.md.template` L33 (`.ruckus/known-pitfalls.md` → `.roughly/known-pitfalls.md`) and `skills/setup/templates/known-pitfalls.md.template` L6 (`/ruckus:build` and `/ruckus:fix` → `/roughly:build` and `/roughly:fix`). Existing `{{PLACEHOLDER}}` markers and the unchanged `claudeignore.template` / `settings.json.template` are preserved verbatim. Folded into PR #19 as a P1 follow-up rather than a separate PR because deferring would have shipped a broken first-install experience for new `/roughly:setup` runs (E02.S2.4)
- Active documentation renamed `Ruckus` → `Roughly`: `README.md` (title, opening, install commands `/plugin marketplace add nickkirkes/roughly` + `/plugin install roughly@nickkirkes`, clone path `~/.claude/plugins/roughly/`, all 22 `/roughly:*` references in code blocks and prose), `CLAUDE.md` (title, opening, structure table prose, `/roughly:setup` reference, `roughly-clone` test path), `CONTRIBUTING.md` (title `# Contributing to Roughly`, clone paths use `roughly-clone` at L8 and L54, 4 `/roughly:*` CLI examples). Zero unintentional `ruckus` residue — the only remaining `ruckus` mentions in active docs are inside the new "Migrating from ruckus (v0.1.3) to roughly (v0.1.4)" subsection (E02.S2.5)
- Two pitfalls captured during S2.5 wrap-up in `.roughly/known-pitfalls.md` via the documented Stage 8 flow per ADR-006 (E02.S2.5 commit `4ff8e5a`)
- Repo-relative paths used in plan verify commands (small docs cleanup discovered during the rename pass) (E02.S2.5 commit `7b6ad8f`)

### Added

- **NEW v0.1.4 upgrade-migration step in `skills/upgrade/SKILL.md`** — detects existing `.ruckus/` directories in user projects and migrates to `.roughly/`. 10-point spec covers: git-vs-plain-`mv` detection, `.ruckus/.migration-in-progress` marker file for partial-failure idempotency, conflict-or-resume branching with explicit abort behavior, idempotent moves, version-line identifier rewrite, anchored boilerplate regex with warn-on-skip, literal-substring CLAUDE.md update with displayed match counts, interactive prompt for user-extra files, marker cleanup, idempotency contract on re-runs (E02.S2.2)
- **Pre-flight migration check in 6 skills** (`build`, `fix`, `review`, `review-plan`, `review-epic`, `setup`) — detects legacy `.ruckus/.migration-in-progress`, `.ruckus/known-pitfalls.md`, or `.ruckus/workflow-upgrades` state and aborts with a redirect to `/roughly:upgrade`. Protects v0.1.3 users who install the new plugin without first running the migration. Beneficial scope expansion beyond the original S2.2 spec (E02.S2.2)
- **Doc-writer path-string sync note in `agents/agent-preamble.md` HTML comment** — exceptions paragraph now explicitly notes that `doc-writer.md` L22 (write-target reference) requires path-string sync whenever the known-pitfalls.md path changes, even though doc-writer's pattern is genuinely different from the preamble-inlining consumers. The L5–L8 manual-sync target list remains unchanged (E02.S2.3)
- **Two new pitfalls captured in `.roughly/known-pitfalls.md`** during S2.3 implementation, recorded via the documented Stage-8 wrap-up flow (E02.S2.3 follow-up commit)
- **Structural Stop-hook (`.claude/hooks/verify-all.sh`)** — fires after every Claude turn in the dogfood repo, reports drift on: stale `.ruckus/known-pitfalls` references in `agents/`, skill bodies > 300 lines, agent bodies > 500 words, and HTML comment integrity in `agents/agent-preamble.md`. Non-blocking and informational; no-op outside the plugin repo. Aligns with ADR-005 maturity-check `stop-hook-v1` (E02.S2.3 scope expansion)

### Fixed

- `skills/fix/SKILL.md` post-S2.2-merge line count (301) reduced to 299 by collapsing the standalone "Format per entry" sentence in the MATURITY CHECKS section into the preceding "Check IDs are versioned" paragraph. Restores compliance with CLAUDE.md's 300-line skill body cap. No behavior change (commit `b2fa658`)
- Stop-hook script gracefully falls back to a heredoc-based JSON emission when `jq` is unavailable on the user's system (commit `1a01f55`)

### Notes

- Behavior is identical to v0.1.3 across all 9 skills and 7 agents. The plugin's pipeline gates, subagent dispatch patterns, two-stage review (ADR-007), Opus-for-epic-reviewer-only model selection (ADR-008), and runtime-context-loading from CLAUDE.md / known-pitfalls.md (ADR-006) are all preserved unchanged.
- Prior CHANGELOG entries (v0.1.0 through v0.1.3) retain `ruckus` naming as historical fact.
- ADR body text in ADR-004/005/006/008 retains its original `/ruckus:*` and capital-R `Ruckus` references as historical decision text. v0.1.4 footnotes were appended to those four ADRs in E02.S2.5.
- `docs/plans/**` historical implementation plans retain their original naming as historical fact.

### Migration

If you were using the previous `ruckus` plugin, follow these steps once per machine and once per project (mirrored in the README's "Migrating from ruckus" section):
1. Install the new plugin under the `roughly` name: `/plugin marketplace add nickkirkes/roughly` followed by `/plugin install roughly@nickkirkes`.
2. Run `/roughly:upgrade` from each project that previously used `/ruckus:*`. The upgrade detects the legacy `.ruckus/` directory and migrates `known-pitfalls.md`, `workflow-upgrades`, and any path references in your root `CLAUDE.md` to `.roughly/`. The migration is resumable on partial failure via the `.ruckus/.migration-in-progress` marker.
3. Optionally uninstall the old plugin: `/plugin uninstall ruckus`. The new and old plugins can coexist temporarily, but only `/roughly:upgrade` runs the migration; the old `/ruckus:*` commands continue to operate on the legacy paths until uninstalled.

The `### Migration` section is a deliberate departure from Keep-a-Changelog convention (Added/Changed/Deprecated/Removed/Fixed/Security only). The rename is the one and only release where existing users must take explicit action — the section's prominence outweighs strict convention conformance.

## [0.1.3] — 2026-04-28

### Fixed

- Upgrade skill no longer offers to install plugin-shipped agents into user projects — agents ship with the plugin and are loaded via `subagent_type`, never copied (E01 audit follow-up)

### Changed

- Setup domain example: replace cannabis-specific example with general-audience e-commerce example

### Added

- New pitfall in `.ruckus/known-pitfalls.md`: plugin-shipped agents must not be inventoried for user installation
- E01 epic audit report: 45 ACs evaluated, 42 MET, 3 PARTIAL (cosmetic), 0 NOT MET

## [0.1.2] — 2026-04-27

### Changed

- **Breaking:** Rename `docs/claude/` to `.ruckus/` for unambiguous plugin ownership (E01.S1)
  - `docs/claude/known-pitfalls.md` → `.ruckus/known-pitfalls.md`
  - `docs/claude/.workflow-upgrades` → `.ruckus/workflow-upgrades` (dropped leading dot)
- Eliminate secondary `docs/claude/CLAUDE.md` — root `CLAUDE.md` is now the sole copy (E01.S1)
- Setup writes `CLAUDE.md` directly to project root instead of `docs/claude/CLAUDE.md` + root copy (E01.S1)
- Doc-writer agent writes to root `CLAUDE.md` and `.ruckus/known-pitfalls.md` (E01.S1)
- Update all agent preambles and skill references to use `.ruckus/` paths (E01.S1)
- ADR-005 and ADR-006 updated with footnotes noting the directory rename (E01.S1)
- fix/SKILL.md description: "Self-upgrades" → "Offers to create" investigator agent (E01.S8)

### Fixed

- Cap four unbounded pipeline loops in build and fix skills (E01.S2):
  - Stage 4 review-plan retry: "2 consecutive" → "2 total" to prevent bypass via alternating results
  - Stage 5c question re-dispatch: add max-2 cap with human escalation
  - Stage 6 review-fix loop: add max-2-cycle cap with human escalation
  - Stage 6 gate: disambiguate "address warnings" with explicit action and one-re-review limit
- Add compaction-resilience to build and fix pipelines (E01.S3):
  - Stage 5 re-validates plan file path after context compaction
  - Stage 5d preserve list includes task ID list
  - Post-Stage 7 compaction boundary preserves feature summary, files changed, and verification verdict
- Disambiguate error handling in pipeline quality checks and review-plan (E01.S4):
  - Stage 5c quality check: split "fix OR re-dispatch" into task-owned auto-fix (max 2) vs external escalation
  - Summary line updated from ">2 attempts" to "after 2 auto-fix attempts" for consistency
  - review-plan: add `.ruckus/` prefix to known-pitfalls.md path reference
  - review-plan: return NEEDS REVISION when CLAUDE.md missing, note gap when known-pitfalls.md missing
- Setup hardening: explicit gate enforcing all 6 required fields before file creation, define "gap" for enrich mode, explicit formatter row removal when no formatter provided (E01.S7)
- Upgrade hardening: preserve user-added hooks when merging settings.json (E01.S7)

### Added

- Upgrade migration step: detects `docs/claude/` in existing projects and offers to move files to `.ruckus/` (E01.S1)
- `.ruckus/` row added to CLAUDE.md structure table (E01.S1)
- Agent preamble drift documentation: explain why static-analysis and doc-writer differ from canonical preamble (E01.S5)
- Implementer-prompt sync comment noting relationship to agent-preamble.md (E01.S5)
- Upgrade preamble drift check: flags agents with stale context-loading instructions (E01.S5)
- Audit-epic token budget batching: batch per-story reviews in groups of 5 for epics with 10+ stories (E01.S6)
- README: review vs review-plan relationship explanation (E01.S8)
- README: context management troubleshooting entry for large builds (E01.S8)
- README: token usage table notes compaction savings (E01.S8)
- README: inline "maturity" definition on first use in Quick Start (E01.S8)

## [0.1.1] — 2026-04-24

### Fixed

- Prevent plan mode hijack in build/fix pipelines — Claude Code's built-in plan mode was intercepting Stage 3→4 flow and skipping review-plan entirely (#8)
- Move marketplace.json to `.claude-plugin/` where Claude Code expects it — root placement caused "Marketplace file not found" errors
- Use `./` source path in marketplace.json for schema validation

### Changed

- Tighten README opener and Quick Start bridging text
- Rename "Self-Upgrading" section to "Upgrade Checks"
- Trim redundant build pipeline bullets (covered in How It Works)
- Modify install instructions in README

### Added

- "Built with Ruckus" invitation section in README
- Issue-first step to CONTRIBUTING PR process
- CODE_OF_CONDUCT.md (Contributor Covenant v2.1)

## [0.1.0] — 2026-04-22

First public release.

### Skills (9)

- `/ruckus:build` — Feature implementation pipeline (8 gated stages)
- `/ruckus:fix` — Bug fix pipeline with investigation stage
- `/ruckus:review` — Parallel 3-agent code review
- `/ruckus:review-epic` — Pre-implementation epic review (Opus)
- `/ruckus:audit-epic` — Post-implementation epic audit with AC verification
- `/ruckus:verify-all` — Type check + test + build verification loop
- `/ruckus:review-plan` — Plan verification (dispatched as subagent)
- `/ruckus:setup` — Project bootstrap with maturity detection
- `/ruckus:upgrade` — Update installed files from plugin templates

### Agents (7)

- `discovery` — Feature research and scoping
- `investigator` — Bug diagnosis via code tracing
- `epic-reviewer` — Cross-story epic review (Opus)
- `code-reviewer` — Code quality and security review
- `static-analysis` — Toolchain verification
- `silent-failure-hunter` — Error handling audit
- `doc-writer` — Documentation updates

### Pipeline Features

- Subagent-per-task implementation with two-stage review after every task
- Mandatory plan review via blocking subagent dispatch
- Explicit override protocol for plan review gate (requires human to say "override")
- UI task detection per-task via `UI: yes/no` flag (loads frontend-design automatically)
- Context compaction boundaries after Stages 4, 5, 6 in build and fix pipelines
- Abort handling with staged cleanup based on pipeline progress
- Plan file validation pre-check before Stage 4 dispatch
- MANDATORY markers on Stages 6 and 7
- Self-upgrading maturity checks with versioned IDs
- CLAUDE.md quality enforcement at implementation start

### Templates

- Stack-aware section comments in claudeignore.template (delete-if-not-using guidance)
- Example pitfall entries in known-pitfalls.md.template
- Plan naming convention: `docs/plans/<feature-name>-plan.md` and `docs/plans/fix-<issue>-plan.md`

### Agents & Prompts

- Shared agent-preamble.md as canonical sync reference for project context loading
- Implementer prompt and spec-reviewer checklist inlined into build/fix SKILL.md (eliminates plugin-relative file path dependencies)
- Compressed implementer prompt from ~170 to ~80 words

### Documentation

- CLAUDE.md — contributor guide with conventions, structure, pitfalls
- CONTRIBUTING.md — fork/test/PR workflow and code standards
- 8 Architecture Decision Records (ADR-001 through ADR-008)
- README with installation, quick start, first-feature walkthrough, pipeline docs, troubleshooting, token usage
- `disable-model-invocation: true` on all pipeline and coordinator skills
